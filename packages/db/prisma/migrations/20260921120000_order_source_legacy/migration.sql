-- Источник «Прежняя ERP» для импортированных заказов (docs/STATUS.md, «Выгрузка заказов»).
-- Отдельное значение, а не OTHER: архивных заказов 3017 против 16 рабочих, и без
-- своего источника их не отфильтровать в списке и не отличить от заказов «прочее».
-- Добавление значения в enum данные не трогает и откатывается только новой миграцией.
ALTER TYPE "OrderSource" ADD VALUE 'LEGACY';
