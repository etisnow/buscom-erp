# Статус проекта

> Читать в начале каждой сессии. Обновлять в конце: отметить сделанное, дописать проблемы, передвинуть «Следующий шаг».
> Требования — `docs/PRD.md`, принятые решения — `docs/DECISIONS.md`.

**Обновлено:** 2026-09-26
**Текущий этап:** 1 — Заказы (M1 + M8) закрыт; сверх него сделаны M2 (клиенты), M3 (товары) и M9 (поставщики), а в базу перенесены клиенты и заказы из прежней ERP
**Ветка для работы:** `development` — коммиты идут туда. В `main` вливает владелец по явной просьбе, потому что пуш в `main` выкатывает на боевой сервер. Деплой на прогоны CI по `development` не реагирует (`docs/DECISIONS.md`, запись про ветки)
**Следующий шаг:** бой наполнен (21.09.2026). Выкат `9c7248e` прошёл, затем данные разработки перенесены одним SQL-файлом (`misc/prod-import.sql`, одна транзакция, проверен на копии пустых таблиц с откатом): 4342 клиента с адресами, 215 товаров с сайта с опциями, 3019 заказов из прежней ERP с позициями, платежами и журналом, включая четыре «В работе» (№3016, 3017, 3019, 3020); сумма 84 332 724,71 ₽ сошлась. Не переносились тестовый заказ №3018 с клиентом «тест», тестовый поставщик и демо-товары (в бою свои 6 демо остались). Нумерация новых заказов — с №3021. Файл с сервера удалён. SSH: ключ `~/.ssh/id_ed25519_buscom` этой машины добавлен пользователю `deploy`, вход — `ssh buscom-prod`. Дальше: всё, что требовалось для запуска, сделано (24.09). КИТ (GTD) в справочнике ТК в бою — владельцем через интерфейс. Восстановление из бэкапа проверено 24.09 (раздел «Бэкапы в бою заведены»). Копия бэкапов вне сервера — на машине владельца (`scripts/pull-backups.ps1`, Планировщик, 24.09); Sentry и алерт «нет заказов» не нужны (решение владельца). Почта (SMTP), пароль администратора, демо-данные и учётка партнёра в бою сделаны владельцем 24.09. Выкат 24.09 прошёл (коммит `39fc64c`): галереи, описания и запрет индексации в бою, подробности — раздел «Выкат 24.09.2026». Бэкапы заведены 24.09 (ежедневно без картинок — 1,2 МБ, картинки отдельно по воскресеньям), см. раздел «Бэкапы в бою заведены». Ближайшее к решению: копия бэкапов вне сервера — последнее невыполненное требование PRD по бэкапам.

Этапы 0 и 1 закрыты целиком — экраны пройдены руками, тесты прогнаны, CI на GitHub зелёный, ветка `etap-0-ui-auth` влита в `main` (PR #1). Дальше этап 2 по PRD, но он упирается в открытые вопросы к заказчику (платформа сайта, способ приёма заказов). Пока их нет, полезное без него: убрать демо-данные и заполнить SMTP.

Пока домен был не привязан, сделано то, что не зависит от сервера (подробности — ниже и в `docs/DECISIONS.md`):

- `3aa41b7` — блокировка входа считает именно неудачные попытки и по email (PRD, M8); попутно строка списка заказов кликается целиком;
- `8398998` — выгрузка списка заказов в CSV, первый кусок M7.

Обе правки в `main`, CI зелёный. На боевом сервере после выката появится новая таблица `FailedLogin` — миграция накатывается одноразовым контейнером, руками ничего делать не надо.

**База разработки теперь общая для обеих машин** (21.09.2026): `buscom_erp_dev` в том же Postgres, что и боевая, доступ по SSH-туннелю (`pnpm db:tunnel`, локальный порт 5433). Локальные базы двух машин расходились по состоянию — это их лечит. Роль `buscom_dev` в боевую базу не пускается (с `buscom_erp` снят `CONNECT` у `PUBLIC`, проверено живой попыткой). **Содержимое — копия боевой базы** (перезалито 22.09.2026): 3019 заказов, 4342 клиента, 221 товар, 215 картинок, 3 поставщика. Первая заливка шла с локальной базы первой машины и затёрла работу второй — четыре действующих заказа «В работе» (№​3016, 3017, 3019, 3020, коммит `cca6866`); в dev под номерами 3019 и 3020 лежали демо-заказы, то есть нумерация двух машин разошлась. По решению владельца dev залит целиком копией боя: четыре заказа восстановлены со своими номерами, нумерация совпадает с боевой (следующий — №3021). Цена решения: исчезли демо-заказы 3018–3031 с их клиентами и тестовая позиция с поставщиком в №​3030. База пересоздана с владельцем `buscom_dev` и залита под ним же — иначе таблицы принадлежали бы суперпользователю и роль разработки потеряла бы доступ. Миграция `20260922200000_carriers_backfill` накачена поверх (в бою её пока нет) — в справочнике 11 перевозчиков. Точки отката на сервере: `/tmp/dev-before-catalog.sql.gz` и `/tmp/dev-before-refill.sql.gz` — удалить, когда станут не нужны. Как подключить вторую машину — `docs/DEV-DB.md`, почему так — `docs/DECISIONS.md`.

Хвост закрыт выкатом `93e1580` (21.09.2026, 22:5x МСК): публикация порта Postgres на loopback теперь в `main`, то есть туннель переживает деплои. Проверено после выката: дерево на сервере чистое, порт слушает только 127.0.0.1, боевая ERP отвечает 200, туннель жив, роль `buscom_dev` в боевую базу по-прежнему не пускается. Этим же выкатом в бой уехала правка позиции заказа (`12e6ebf`) — миграций в нём нет, схема боя не менялась. Для работы без сети есть `pnpm db:pull` — копия общей базы в локальную.

**База на второй машине разработки заведена** (20.09.2026): нативный PostgreSQL 16 (Docker на ней нет), роль `buscom`, база `buscom_erp`, четыре миграции накатаны `prisma migrate deploy`, сид отработал. Вход проверен живым запросом к `/api/auth/sign-in/email`: 200, роль ADMIN, cookie сессии ставится.

Симптом на будущее, если база не поднята: страница `/login` открывается (без cookie сессии Better Auth в базу не ходит), а вход падает `P1001` на `db.failedLogin.findMany` — выглядит как баг приложения, но это отсутствующая БД. Проверять `pnpm prisma migrate status`. Интеграционные тесты по живой БД в таком случае пропускаются — в прогоне их 42.

Ещё один симптом (21.09.2026, машина разработки): `/orders` падает `PrismaClientValidationError` — «Invalid value for argument `type`. Expected DictionaryType» на `dictionaryItem.findMany` с `ORDER_SOURCE`. Это не баг кода, а отставшая локальная БД: семь миграций (от `20260921150000_suppliers` до `20260922120000_product_categories`) не были накатаны. Лечение: `pnpm prisma migrate deploy` + `pnpm db:generate`. Одного перезапуска `pnpm dev` мало — Turbopack отдаёт скомпилированный чанк со старым клиентом из `.next`, поэтому её надо удалить и стартовать заново.

**Первый выкат этих правок упал: на сервере кончилось место** («no space left on device» на `docker compose pull`; диск был занят на 99%, свободно 104 МБ). Приложение при этом продолжило работать — старая версия, как и задумано. Причина не в коде правок, а в уборке образов: она стояла после проверки живости и с фильтром «старше 168 часов», под который выкаты одного дня не попадают. Починено коммитом `5ef48c8` — чистка идёт до скачивания, см. `docs/DECISIONS.md`.

Повторный выкат в 17:32 МСК прошёл целиком: уборка освободила 3,6 ГБ до скачивания (диск 99% → 62%) и ещё 1,78 ГБ после, приложение ответило на порту 81. На сервере теперь версия с блокировкой входа, таблицей `FailedLogin` и выгрузкой в CSV. Снаружи она по-прежнему доступна только по имени сервера — до привязки домена.

## Этап 0 — Фундамент

- [x] Next.js 16 + TypeScript strict + Tailwind 4, pnpm
- [x] Prisma 7 схема по PRD + начальная миграция `20260919000000_init`
- [x] Доменный слой: деньги, статусная машина, итоги заказа, нормализация телефона (+ Vitest)
- [x] `src/server`: env (Zod), Prisma-клиент с pg-адаптером, конфиг Better Auth, route handler `/api/auth/[...all]`
- [x] CLAUDE.md, docs/PRD.md, docker-compose (Postgres), GitHub Actions CI, prettier
- [x] Миграция `20260919000000_init` применена на живой БД (нативный Postgres 16, роль `buscom`, БД `buscom_erp`; Docker на машине нет — см. DECISIONS). `prisma migrate status` — «up to date»
- [x] shadcn/ui (пресет `radix-nova`) + базовый layout: левое меню (Заказы, Клиенты, Товары, Администрирование — по правам), шапка с глобальным поиском и меню пользователя
- [x] Страница `/login` (email + пароль через Better Auth), `src/proxy.ts` редиректит без сессии на `/login?next=…` (кроме `/api/auth/*` и `/api/integrations/*`)
- [x] В разработке форма входа подставляет `SEED_ADMIN_EMAIL` / `SEED_ADMIN_PASSWORD` из `.env` (20.09.2026). Подстановкой управляет `pickDevLoginCredentials` в `src/server/env.ts`, и решает она по `NODE_ENV`, а не по наличию переменных: пароль уходит в разметку страницы входа, поэтому в бою подстановки не должно быть ни при каких настройках (+ 3 теста). Переменные необязательные — в боевом compose их получает только контейнер миграций, до `app` они не доходят
- [x] Хелпер `requireUser(roles?)` в `src/server/session.ts` — сессия + роль + `isActive`; `ForbiddenError` для 403
- [x] Сид `prisma/seed.ts` + скрипт `pnpm db:seed`: первый ADMIN из env (`SEED_ADMIN_EMAIL`, `SEED_ADMIN_NAME`, `SEED_ADMIN_PASSWORD`) и демо-каталог. Справочников в схеме пока нет — сеять нечего
- [x] Заглушки экранов `/orders`, `/customers`, `/products`, `/admin`, чтобы меню никуда не вело в 404; `/` редиректит на `/orders`
- [x] Проверено на живой БД: вход ADMIN'ом (cookie сессии ставится, `role` и `isActive` приходят), `/orders` без сессии → `/login?next=/orders`, `/login` с сессией → `/orders`, менеджер не видит пункт «Администрирование» и не открывает `/admin`, у деактивированного любой экран редиректит на `/login`
- [x] Проверить UI глазами в браузере — сделано 2026-09-20, см. «Проход по экранам»
- [x] CI на GitHub зелёный: прогон #4 по `main` (merge-коммит `61ab909`, 2026-09-20) — все 13 шагов, включая `pnpm test` с 29 интеграционными тестами по живому Postgres и `pnpm build`. Два прогона 19.09 падали на `pnpm typecheck` — то есть до тестов CI раньше вообще не доходил, и параллельность тестов по БД он поймать не мог

## Этап 1 — Заказы (M1 + M8)

Критерии приёмки — PRD, «Экраны и критерии приёмки (P0)».

- [x] `src/domain/order/payment-status.ts` — вычисляемый статус оплаты, остаток к оплате, признак полной оплаты для автоперехода в PAID (+ 15 тестов)
- [x] `src/domain/order/discount.ts` — лимит скидки 10% от суммы до скидок, выше — только HEAD/ADMIN (+ 17 тестов)
- [x] `src/domain/sla.ts` — рабочие минуты пн–пт 09:00–18:00 МСК, нормативы по статусам, `slaState` и формат «2 раб. дн. 3 ч» (+ 28 тестов)
- [x] Сервис заказов в `src/server/orders/`: создание вручную, «взять себе», переназначение, смена статуса, правка позиций, оплаты, комментарии — каждое действие в транзакции с `OrderEvent`
- [x] Доменные правила к нему: `editing.ts` (кто и когда правит состав) и `reservation.ts` (когда заказ держит резерв) + 22 теста. `reservation.ts` удалён 2026-09-20 вместе со складом
- [~] ~~Резерв остатка по правилам PRD~~ — **отменено 2026-09-20**: склада у компании нет, весь блок удалён (см. «Склад убран из проекта»)
- [x] `/orders` — список: колонки по PRD, вкладки-виды со счётчиками, фильтры по статусу (множественный), менеджеру, источнику, периоду и оплате, поиск одним полем, пагинация по 50. Всё состояние в URL; просроченные подсвечены. Вид «К отгрузке» убран 2026-09-20 вместе со складом — все открывают «Все»
- [x] Миграция `20260920000000_order_paid_and_sla_cache`: `Order.paidKopecks` и `Order.slaDueAt` — чтобы фильтры по оплате и просрочке были запросом, а не перебором (см. DECISIONS)
- [x] `/orders/[number]` — карточка: шапка со статусом, оплатой и кнопками ровно по `availableTransitions`, «Взять себе», переназначение руководителем, редактор позиций с подбором товара и предупреждением о лимите скидки, диалог отмены с причиной из справочника, блок оплат с подстановкой остатка, доставка, комментарий клиента и лента истории с полем внутреннего комментария
- [x] `/orders/new` — ручное создание: источник, поиск существующего клиента по телефону/имени/ИНН или новый, подбор позиций из каталога, доставка, скидка с предупреждением о лимите. Заказ сразу в IN_PROGRESS с автором в роли менеджера
- [x] `/admin/users` — список сотрудников, заведение с временным паролем, смена роли, отключение со сбросом сессий, выдача нового пароля. Себя отключить или разжаловать нельзя
- [x] `/admin/dictionaries` — причины отмены и транспортные компании (позиции гасятся, а не удаляются), лимит скидки, SLA по статусам, реквизиты продавца
- [x] `/admin/integration` — журнал входящих с фильтром по статусу, просмотром сырого JSON и кнопкой «Повторить» для упавших
- [x] Миграция `20260920010000_dictionaries_and_settings`: `DictionaryItem` и `Setting`. Настройки читаются через `src/server/settings/service.ts`, умолчания — в `src/domain/settings.ts`, поэтому пустая база работоспособна
- [x] `POST /api/integrations/site/orders` — контракт v1: HMAC-подпись сырого тела со сравнением в постоянном времени, сырой payload в `IntegrationInbox` до разбора, идемпотентность по `(source, externalId)`, поиск товара по `externalProductId` затем `sku`, предоплата с сайта, предупреждение о расхождении сумм. Все пять кодов ответа из PRD проверены живыми запросами (+ 23 теста)
- [x] PDF: счёт на оплату (с суммой прописью и реквизитами из настроек); комплектовочный лист был и убран 2026-09-20 вместе со складом. Генерирует pdfmake со шрифтом Roboto из самого пакета — бинарников в репозитории нет; кириллица в готовых файлах проверена
- [x] Сброс пароля по email: `/forgot-password` и `/reset-password`, письмо через nodemailer, SMTP из env. Без SMTP ссылка печатается в лог сервера, чтобы разработка не требовала почтового сервера. Проверено целиком: ссылка → смена → старый пароль не работает → токен одноразовый
- [x] Таблицы справочников и настроек заведены; лимит скидки и SLA из настроек реально применяются сервисом — проверено на живой БД (лимит 10% → 25% меняет приём скидки, SLA 5 минут меняет дедлайн)

**Заказ поставщику** (21.09.2026, дополнено 22.09.2026): в карточке заказа у каждого поставщика кнопка «Заказ поставщику» — готовый текст с кнопкой копирования. Текст собирает `buildSupplierRequest` (домен, 13 тестов), проверено в браузере на заказе №​3030. Шаблон фиксирован в коде, см. `docs/DECISIONS.md`. Подпись «Покупатель» — полные реквизиты клиента заказа (не нашей компании): юр. название, ИНН/КПП, юр. адрес, ОГРН, банк с р/с, к/с, БИК, телефон — тот же `CustomerRequisites`, что в карточке клиента и в счёте. Для проверки в заказ №​3030 добавлена позиция «Цанговое крепление сидушки» с поставщиком «Прайд (нал.)» — только в dev-базе, убрать можно из карточки. С 24.09.2026 цены в тексте — конечная стоимость с «Экономикой цены» (снимок `purchaseCostKopecks`), а не номинал поставщика; расходы на заказ — отдельной строкой перед «Итого» и входят в него.

**Настройки почты в админке** (22.09.2026): раздел «Почта» в `/admin/dictionaries` — хост, порт, шифрование, пользователь, пароль, «От кого». Введённое главнее переменных `SMTP_*`, они остались запасным путём. Пароль в браузер не отдаётся (проверено поиском по разметке), пустое поле оставляет сохранённый (+ 11 тестов). Проверено в браузере целиком: сохранение, перезагрузка, повторное сохранение с пустым паролем. Рядом кнопка «Отправить тестовое письмо»: проверяет то, что в полях, ничего не сохраняет, письмо идёт на адрес нажавшего. Проверены оба пути ошибок: пустой хост — «Сначала укажите сервер», несуществующий — текст ошибки nodemailer целиком. Тестовая настройка из dev-базы убрана — в разработке письма по-прежнему идут в лог. В бою сами настройки ещё не заполнены — это остаётся в списке дел, но теперь это делается через интерфейс, а не правкой `.env.production` на сервере.

**Личные настройки и уведомления** (24.09.2026): `/settings`, пункт «Настройки» в меню пользователя. «Почта для уведомлений» (`User.notificationEmail`, пусто — на адрес входа) и подписки галочками (`User.notificationTopics`): новый заказ — отдельно «вручную» и «с сайта»; статус оплаты — отдельно «Частично», «Оплачен», «Переплата»; каждый этап каждого поставщика. Страница и сохранение подписок проверены в браузере. Письма по всем заказам, кроме собственных изменений. Кнопка «Отправить тестовое письмо» — на адрес из поля (даже несохранённый; пусто — адрес входа), не чаще раза в 30 с на сотрудника; без настроенной почты честно говорит, что письма не уходят. Хуки пишут письма в очередь `Notification` в транзакции изменения, отправка — через секунду и повтором раз в минуту (до 5 попыток). Миграции `20260924120000_user_notification_email`, `20260924140000_notifications` и `20260924160000_notification_topics_split` (раскладывает общие подписки первой версии на новые галочки) накачены на общую dev-базу (сверено `migrate diff` — пусто), в бою — со следующим выкатом. Юнит-тесты зелёные; `notifications.db.test.ts` (7 тестов) — локально не запустить (нет `buscom_test` и прав её создать), проверен в CI. В браузере не проверено: запущенный `pnpm dev` держал старый Prisma-клиент — нужен перезапуск с удалением `.next`. Пока SMTP в бою не заполнен, письма только печатаются в лог сервера и помечаются отправленными.

**Поиск товаров без учёта регистра, строки заказов без заливки** (23.09.2026). Поиск в `/products`, выгрузке и подборе товара в заказ шёл через `ILIKE`, а база (`SQL_ASCII`, локаль `C`) кириллицу по регистру не сворачивает: «СИДЕНЬЕ» не находило ничего. Теперь сравнение в приложении, как у клиентов (`matchProductsCaseInsensitive`): «СИДЕНЬЕ» — 17 товаров, «газель» — 14 (по совместимости, раньше только модель целиком). В списке заказов просроченные больше не заливаются красным целиком — красное только время в колонке «В статусе».

**Закупка по опциям и «Подтянуть цены опций»** (23.09.2026). У вариантов опций товара — своя закупка у каждого поставщика (таблица `ProductSupplierOptionPrice`, миграция `20260923210000_product_supplier_option_price`, в dev накатана); в заказе закупка позиции = базовая закупка + закупки выбранных вариантов, дальше — «Экономика цены», снимок, маржа. В карточке товара под поставщиком — «Закупка по опциям» и кнопка «Подтянуть цены опций»: обход всех вариантов на странице vanproject.ru с ценами (13 вариантов — 1,5 с), сопоставление по размеру/стороне/форточке, неоднозначные — выбор из кандидатов. Проверено в браузере на ST101 без сохранения: 3 из 10 подобраны сами, выбор кандидата подставляет цену (7 150 ₽). У ST101 в dev поставщик так и не привязан — карточку владелец заполнял без сохранения. Тесты: 9 на сопоставление по сохранённой форме Ducato L2, 1 по БД на всю цепочку (пройдёт в CI).

**«Подтянуть цену» по варианту товара на vanproject.ru** (23.09.2026). Если цена на странице зависит от выпадающих списков, кнопка не берёт цену первого варианта, а показывает списки в строке поставщика (второй сужается по первому); когда выбрано всё — спрашивает у сайта цену варианта тем же запросом, что браузер, и подставляет. Выбор хранится в паре товар–поставщик (`ProductSupplier.variant`, миграция `20260923190000_product_supplier_variant`, в dev накатана). Проверено вживую: сервис — 9 250 ₽ и 13 200 ₽ на двух вариантах стекла XLWB, неверное сочетание — понятная ошибка, простой товар — 330 ₽ как раньше; в браузере — списки, сужение, автоподстановка 9 250 ₽ (без сохранения). Тесты: 8 на разбор вариантов по сохранённой форме и ответу сайта, 1 по БД на хранение выбора.

**Комиссия с прибыли у поставщика** (23.09.2026). Раздел в карточке поставщика: процент от нашей прибыли по его товарам в заказе (0 — не удерживает) и проверка на примере. Снимок процента — у поставщика в заказе (миграция `20260923170000_supplier_profit_commission`, в dev накатана); сумма считается в марже заказа по снимкам и показывается строкой по каждому поставщику. Проверено в браузере: 20% от 400 ₽ = 80 ₽, сохранение, возврат к «не удерживает» (у «Прайд (нал.)» в dev — 0, как было); маржа №3020 не изменилась — снимок у старого заказа 0. Тесты: 5 на комиссию в марже, 1 по БД на снимок (пройдёт в CI).

**Экономика цены у поставщика и маржа заказа** (23.09.2026). В карточке поставщика — конструктор формулы от номинала к стоимости закупки для нас: проценты (база у каждого шага — номинал или накопленное), сумма за штуку, округление, отдельно — расходы на заказ; проверка на любой цене с расшифровкой. Стоимость для нас — в таблице товаров поставщика, в списке и карточке товара, при выборе поставщика в позиции; в заказ идёт снимком (`OrderItem.purchaseCostKopecks`, `OrderSupplierTrack.orderCostKopecks`, миграция `20260923150000_supplier_price_economics` — в dev накатана, старым позициям снимок = номинал). В карточке заказа — блок «Маржа». Проверено в браузере на dev: формула «+5% → +150 ₽ → округление вверх до 10 ₽, 500 ₽ на заказ» на 300 ₽ даёт 470 ₽ и +500 ₽; сохранение, колонка «Для нас» (50 → 210 ₽), маржа №3020 по старому снимку (1 500 ₽, 50%); формула поставщика «Прайд (нал.)» после проверки возвращена пустой. Тесты: 24 на формулу, 4 на маржу, 1 по БД на снимки (пройдёт в CI). Миграцию `migrate dev` на общей базе не создать (у роли нет прав на теневую базу) — SQL получен `prisma migrate diff --from-config-datasource`.

**Заказы с сайта из писем** (23.09.2026). Сайт — OpenCart; уведомления «Вы получили заказ» идут на `zakaz@erp.bus-com.ru` (Джино, поддомен `erp`; на основном домене MX у Яндекса, ящики Джино там писем не получают). ERP забирает письма по IMAP раз в 2 минуты (в бою; в разработке — кнопка «Проверить почту» в `/admin/integration` или `pnpm mail:poll`), кладёт письмо в журнал интеграции («Сайт (письмо)») и создаёт заказ общим путём контракта v1. Только новые письма (курсор по UID), принятые отмечаются прочитанными, дублей нет. Заказ с ИНН теперь ищет клиента по ИНН, а не по телефону (решение владельца). Проверено живьём на двух настоящих письмах в dev-базе: заказы №3021 и №3022 (№ на сайте 2819, 2820), позиции связались с каталогом по артикулу, повторный проход — «уже принятых: 2». Прогон был до правила про ИНН, поэтому №3022 (ООО «АВТОМАСТЕР-СЕРВИС») в dev привязан к физлицу — тестовые данные, можно отменить. Тесты: 24 на парсер (обезличенные копии писем в `src/domain/integration/fixtures/`), 7 по БД — локально не запускались, пройдут в CI. Хостинг Джино не открывается с машины разработки (VPN): для dev — `IMAP_VIA` через SSH-проброс, см. `scripts/mail-poll.ts`.

**В бою включено** (23.09.2026, выкат `3e099f0`): переменные `IMAP_*` в `.env.production`, в логе `[mail] Ящик подключён` — принимаются письма с этого момента. Осталось: в OpenCart добавить `zakaz@erp.bus-com.ru` в дополнительные адреса для уведомлений о заказах и проверить тестовым заказом с сайта. CI поймал ошибку, которую локальный прогон не видит (тесты по БД здесь пропускаются): в `normalizeInn` пропали обратные слэши, поиск по ИНН не срабатывал; исправлено в `3e099f0`, функция перенесена в домен с обычным тестом.

**Колонки списка заказов** (23.09.2026): из таблицы `/orders` убраны «Менеджер» и «Источник», добавлены «Позиции» (до трёх строк «название × кол-во», дальше «ещё N», полный состав в подсказке) и «Статусы поставщика» (текущий этап по каждому поставщику, пройденный — зелёным). Фильтры по менеджеру и источнику остались. Проверено в браузере на dev-базе.

**Фильтры заказов, ссылка и цена поставщика** (22.09.2026). Фильтры списка запоминаются в cookie, подставляет их сервер редиректом до отрисовки — мигания нет (в логе `GET /orders 307`, следом 200 с фильтрами). «Сбросить» чистит и память. У пары товар–поставщик есть ссылка на товар (миграция `20260922210000_product_supplier_url`) и кнопка «Подтянуть цену» с avito.ru и vanproject.ru (Фургон Проект; живой прогон — 330 ₽, как на сайте). Модалка товара больше не обрезается: колонка сетки диалога теперь `minmax(0,1fr)`, а ширина `sm:max-w-2xl` — прежний `max-w-2xl` перебивался базовым `sm:max-w-sm`, и окно было 384px.

**Цена с Авито — с оговоркой.** Разбор разметки покрыт 14 тестами, но живого прогона не было: с машины разработки Авито отвечает 403 (адрес — выход VPN; маршруты обхода из `scratchpad/avito-bypass.ps1` на момент проверки не были применены). Кнопка при этом честно говорит, что не пустили. Сработает ли с боевого сервера (российский адрес) — покажет первый живой прогон. Обходить защиту не стали.

**Заполнение юрлица по ИНН** (22.09.2026). У поля ИНН — в окне «Новый клиент» и в карточке — кнопка «Заполнить» (и Enter): КПП, название, юр. название и адрес, ОГРН, руководитель из ЕГРЮЛ через DaData (`src/domain/customer/company-lookup.ts`, 12 тестов). В окне заведения найденные реквизиты показаны списком и сохраняются вместе с клиентом. Выкачено в прод, ключ `DADATA_API_KEY` добавлен в `.env.production` и доходит до контейнера (`docker-compose.prod.yml`) — живой прогон ещё предстоит проверить в бою. У поставщиков те же поля (`supplier-fields.tsx`), туда кнопку не ставили — не просили.

**Тема: фирменный зелёный** (22.09.2026). Палитра в `src/app/globals.css` была чисто серой (хрома 0 у каждого токена) — интерфейс казался монотонным. Цвета сняты живьём с bus-com.ru (`getComputedStyle` по кнопкам и ценам) и переведены в OKLCH: `--primary`, `--ring`, `--accent` и весь сайдбар теперь в фирменном зелёном (`#0f8e49` сайта), активный пункт меню подсвечен. `--destructive` и цветные статусы заказов (`status-badge.tsx`) не трогали — не пересекались с задачей. Проверено в браузере, светлая и тёмная тема.

**Новый заказ из карточки клиента** (22.09.2026). Кнопка «Новый заказ» в карточке (видна тем же ролям, что создают заказ — `ORDER_CREATE_ROLES`) ведёт на `/orders/new?customerId=…`. Клиент подставляется в форму уже выбранным (`findCustomerMatch`, `NewOrderForm.initialCustomer`) — искать и выбирать его заново не нужно, остальная форма работает как обычно. Проверено в браузере: клиент из карточки виден в форме сразу.

**Поиск клиента: частичные телефон/ИНН, регистр кириллицы, ИНН в выдаче** (22.09.2026). Раньше `lookupCustomers` (подбор клиента при заказе, слиянии дублей) и `customersWhere` (список `/customers`, он же CSV-экспорт) находили по телефону и ИНН только точным совпадением, а по имени — не находили «БАСКОМ» по запросу «баском» (регистр кириллицы). Теперь:

- при 3+ цифрах в запросе ищет ещё и вхождением (`contains`), полный номер/ИНН по-прежнему проверяется отдельно точным совпадением через нормализацию (`8 916…` = `+7 916…`);
- по имени — без учёта регистра кириллицы через `matchNamesCaseInsensitive` (`src/server/customers/name-match.ts`), не через `ILIKE` — у базы кодировка `SQL_ASCII`/локаль `C`, обычный Postgres это не умеет; подробности и компромисс — `docs/DECISIONS.md`;
- в выдаче поиска (подбор клиента при заказе, слияние дублей) рядом с телефоном теперь виден ИНН, если он есть.

Живой прогон в браузере на dev-базе: «401282» и «421212» нашли одного и того же клиента (середина телефона / середина ИНН), «свм» на странице `/customers` нашёл «ООО "СВМ"». Тесты — `lookup.db.test.ts`, `list.db.test.ts` (требуют `RUN_DB_TESTS=1`, локально не запускались, пройдут в CI).

**Реквизиты юр. лица: переименовано и собрано в одном месте** (22.09.2026). «Реквизиты для договоров» → «Реквизиты юр. лица»; ИНН и КПП переехали из общей формы клиента внутрь этого блока (карточка клиента и форма «Новый клиент»). В форме «Новый клиент» блок реквизитов раньше был только для чтения (после кнопки «Заполнить» по ИНН) — теперь такой же редактируемый набор полей, что и в карточке (`CUSTOMER_REQUISITES_FIELDS`, общий список из `src/domain/customer/requisites.ts`, раньше дублировался). Проверено в браузере: карточка клиента и форма заведения.

**Кнопки: зелёная «Новый заказ», красные «Удалить»** (22.09.2026). «Новый заказ» в карточке клиента был серым `outline` — переведён на обычный (зелёный) вариант, как везде в проекте. Все кнопки и иконки удаления/уборки строки (было вперемешку `outline`/`ghost`) переведены на `variant="destructive"` — по всему проекту: карточки клиента и поставщика, товар (поставщики, картинка, категории), позиции заказа (новый и существующий), адреса, опции товара, этапы поставщика, справочники. Кнопки подтверждения в диалогах уже были красными, не трогал.

**Действия и артефакты у поставщика** (22.09.2026). В карточке поставщика — раздел «Действия и артефакты»: чекбоксы с описанием, реестр функций в `src/domain/supplier/actions.ts`. Сейчас две: «Заказ поставщику» (стала опциональной, по умолчанию включена у всех — бэкфилл через `@default` в миграции) и новая «Прикрепить счёт поставщика клиенту» — загрузчик PDF/фото до 15 МБ, файл привязан к паре заказ–поставщик (`OrderSupplierDocument`), отдельно от трека этапов, чтобы не пропадал при правке состава заказа. Выключенное действие в карточке заказа просто не показывается. Подробности и почему так — `docs/DECISIONS.md`.

**Заказ поставщику (наши цены)** (24.09.2026): третье действие поставщика `SUPPLIER_REQUEST_OUR_PRICES`, по умолчанию выключено — включается чекбоксом в «Действиях и артефактах». Тот же текст `buildSupplierRequest`, но цена позиции — наша цена продажи за штуку (`OrderItem.priceKopecks`, до скидки на позицию), без строки «Расходы на заказ». В браузере не проверялось.

Живой прогон в браузере на dev-базе (заказ №3020, поставщик «Прайд (нал.)»): включил чекбокс, кнопка загрузки появилась в заказе; загрузил тестовый PDF — появилась ссылка с именем и размером, скачался с верным `Content-Type`; удалил — ссылка пропала. Оба события (прикреплён/удалён) в журнале заказа. Чекбокс «Прикрепить счёт…» у «Прайд (нал.)» оставлен включённым — можно выключить из карточки поставщика, тестовый файл убран.

Миграция `20260922220000_supplier_actions_and_documents` создана через `prisma migrate diff` в обход shadow-базы (ни у общей dev-базы, ни у локальной роль не имеет `CREATEDB`) и применена `migrate deploy` к обеим базам. Тесты — `src/domain/supplier/actions.test.ts`, `src/domain/order/supplier-document.test.ts` (юнит, прошли), `src/server/orders/supplier-documents.db.test.ts` и два новых в `suppliers.db.test.ts` (требуют `RUN_DB_TESTS=1`, локально не запускались — в этой среде разработки ни у одной базы нет `CREATEDB`, не только у shadow-базы, поднять тестовую БД не вышло; пройдут в CI).

**Замечено по ходу:** в этой среде разработки гидратация страницы заказов занимает до десятка секунд: селекты всё это время пустые, а клики не работают. При проверке правок в браузере это легко принять за сломанный код — надо ждать, пока появятся подписи «Любой» в фильтрах.

## Сделано сверх этапа 1 (модули M2 и M3 из PRD)

- [x] `/products` — каталог: поиск, фильтр по категории, заведение и правка товара. Фильтр по нехватке и правка остатка в таблице убраны 2026-09-20 вместе со складом
- [x] `/customers` — база клиентов: поиск по имени, телефону, email и ИНН, фильтр по типу, сумма покупок по закрытым сделкам
- [x] `/customers/[id]` — карточка: данные с нормализацией телефона при сохранении, адреса доставки с одним адресом «по умолчанию», история заказов, слияние дублей вручную
- [x] Заведение клиента кнопкой «Новый клиент» (21.09.2026): тип, имя, телефон, email, ИНН/КПП, контактное лицо или паспорт, комментарий и сразу адреса доставки. Дубль по телефону не заводится — форма ведёт в карточку существующего
- [x] Реквизиты юрлица для договоров в `Customer.requisites` (Json): юр. название и адрес, ОГРН, банк, БИК, р/с, к/с, руководитель, бухгалтер
- [x] Удаление клиента (21.09.2026): руководителем и администратором, с подтверждением; клиента с заказами сервер удалить не даёт
- [x] Импорт клиентской базы и заказов из прежней ERP — `pnpm import:customers` и `pnpm import:orders`, оба идемпотентны (см. разделы «Выгрузка … из прежней ERP»)
- [x] **M5: груз и дата отгрузки в доставке** (24.09.2026). В блоке «Доставка» карточки заказа — «Дата отгрузки», «Вес, кг» и «Габариты, см» (длина × ширина × высота). Хранятся целыми: `Order.cargoWeightGrams`, `cargoLengthCm`, `cargoWidthCm`, `cargoHeightCm` (миграция `20260924230000_order_cargo`, накатана на dev-базу и `buscom_test`; в бой уедет с выкатом — одноразовым контейнером миграций). Разбор ввода — `src/domain/order/delivery.ts` (`parseWeightKg`, `parseSideCm`, `formatCargo`, 8 тестов), сервис `updateOrderDelivery` пишет прежние значения в журнал (3 теста по живой БД). Дата отгрузки при переходе в «Выполнен» по-прежнему ставится сама, если пуста. Помощники дат по Москве (`parseDateInput`, `toDateInput`, `moscowParts`, `moscowMidnight`) переехали из аналитики в `src/domain/datetime.ts`. Проверено в браузере на заказе №3016 dev-базы: сохранение, перечитывание после перезагрузки, ошибка на «12,5555» кг; значения потом очищены, в истории заказа остались три записи «Изменена доставка»
- [x] ~~Груз в «Заказе поставщику»~~ — не нужен (решение владельца, 24.09.2026)
- [x] **Транспортная накладная в доставке** (25.09.2026). В блоке «Доставка» — загрузчик PDF/JPEG/PNG/WebP до 15 МБ, файл сохраняется сразу, без «Сохранить доставку»; менять можно, пока заказ не закрыт (как и остальную доставку). Новая модель `OrderDocument` — файлы самого заказа, вид `WAYBILL` (реестр видов — `src/domain/order/order-document.ts`, +4 теста), событие `ORDER_DOCUMENT_CHANGED` («Файл заказа» в истории). Сервис `src/server/orders/order-documents.ts` (замена — новая запись с новым id, как у счёта поставщика), отдача — `/api/order-documents/[id]`. Миграция `20260925150000_order_documents` (только новое) накатана на общую dev-базу (`migrate diff` — пусто), в бой уедет с выкатом; `buscom_test` на этой машине нет — `order-documents.db.test.ts` (3 теста) проверится в CI. Общий загрузчик вынесен в `src/components/orders/document-upload.tsx` — им же пользуется счёт поставщика — и теперь принимает файл перетаскиванием (пунктирная зона с подсветкой). Проверено в браузере на №3016 (dev на :3100 — :3000 занят другим проектом): сброс PDF → ссылка, файл отдаётся с `application/pdf`; сброс `.txt` → «Это не PDF и не картинка»; удаление кнопкой. В истории №3016 остались две записи «Файл заказа» (прикреплён / удалён). **Выкачено 25.09** (`015932c`): приложение healthy, миграция в бою применена
- [x] **Распознавание накладной — «Заполнить из накладной»** (25.09.2026, **выкачено `176eb1a`**: образ собрался — шаг со ссылками в Dockerfile прошёл, приложение healthy, ошибок в логе нет; **в бою кнопка падала** «Cannot find module '@tesseract.js-data/rus/package.json'» (лог сервера, проверка владельцем): сборка копирует только саму модель, без package.json пакета. Исправлено: путь к модели от папки приложения (`LANG_PATH`), а в Dockerfile добавлена проверка распознавания в собранном образе (`scripts/ocr-smoke.mjs`) — сломанный OCR теперь валит сборку, а не кнопку в бою). Выкат `2ffe143` упал на pull: «no space left on device» — остановленный контейнер прошлой миграции держал образ migrate (2,55 ГБ), prune его не удалял; бой остался на `176eb1a`. В deploy.yml перед уборкой и после успешного выката — `compose rm -f migrate`. **Выкачено `07f03c3`**: проверка OCR в образе прошла, приложение healthy, свободно 3,8 ГБ (было 1,4). Кнопку в бою проверяет владелец. **Дальше стоит ужать образ migrate** (сейчас это вся стадия сборки: node_modules с dev-зависимостями и .next) — диск 9,8 ГБ. Локально, без облака (решение владельца): PDF с текстовым слоем читается как есть, скан — Tesseract (`tesseract.js`, русская модель `@tesseract.js-data/rus` из пакета, в сеть не ходит) + штрихкоды `zxing-wasm`, PDF рисуется `unpdf` + `@napi-rs/canvas`. Разбор — `src/domain/order/waybill-parse.ts` (+14 тестов на настоящем выводе OCR, персональные данные в фикстуре заменены): ТК из справочника, трек (сверяется со штрихкодом — OCR путает цифры), дата «от …», вес, габариты м→см, «Итог» в стоимость (решение владельца), город назначения — только в пустой адрес. Кнопка подставляет значения в форму и подсвечивает изменённые; сохраняет менеджер. Сервер: `src/server/orders/waybill-recognition.ts`, по одному распознаванию за раз, воркер на запрос. Проверено в браузере на №3016 с образцом «Деловых Линий»: 8 с, номер `26-01211275323` верный (по штрихкоду), дата, вес, длина, ширина, «Итог» верные; **высота 50 вместо 30 — ошибка OCR** на этом скане, её ловит только проверка глазами. Файл потом убран, доставка не сохранялась. Правила сняты с одного бланка «Деловых Линий»: у других ТК номер и поля найдутся хуже — нужны их образцы
  - **Сборка:** Tesseract грузит воркер, ядро и модель по путям, которых трассировка standalone не видит, — пакеты перечислены в `outputFileTracingIncludes` (`next.config.ts`), а в Dockerfile им ставятся ссылки в `node_modules/.pnpm/node_modules` (сборка падает, если пакета нет). Конвейер целиком проверен запуском из `.next/standalone` на Windows. **Docker-образ (alpine) локально не собирался — Docker на этой машине нет**; первая проверка — сборка образа при выкате. После выката проверить кнопкой на заказе. Рабочий образ +~110 МБ (ядро Tesseract 44 МБ, модель 12 МБ, canvas); на сервере свободно 2,0 ГБ диска
  - **Нагрузка:** на сервере 1 ядро и 1,5 ГБ памяти (свободно ~880 МБ); распознавание — ~250–400 МБ и 5–15 с на ядре. Параллельные нажатия ждут очереди

## Сделано из этапа 3 (M7)

- [x] Выгрузка списка заказов в CSV: кнопка «Выгрузить CSV» на `/orders`, маршрут `GET /api/orders/export`. В файл идёт то же, что на экране — те же фильтры и вид, только без пагинации. Формат под Excel с русской локалью: `;`, CRLF, BOM; суммы числами, статусы словами, телефоны с восьмёрки; ячейки-формулы обезврежены. Потолок 10 000 строк, и он виден в имени файла. Проверено 5 тестами по живой БД, 6 доменными и живым HTTP-запросом с сессией
- [x] Выгрузка списков клиентов и товаров: кнопки «Выгрузить CSV» на `/customers` и `/products`, маршруты `GET /api/customers/export` и `GET /api/products/export`. Та же схема, что у заказов: в файл идёт видимый список с теми же фильтрами, без пагинации. Общее вынесено — `EXPORT_LIMIT`, `csvDateTime` и `csvFileName` в `src/domain/csv.ts`, разбор параметров URL в `src/app/(app)/search-params.ts`, условия выборки переиспользуются из списков (`customersWhere`, `productsWhere`). Проверено живым HTTP с сессией (оба файла, фильтры, BOM и CRLF в байтах, без сессии — 307 на `/login`) и 16 тестами: 4 доменных и 12 по живой БД
- [x] Экран «Аналитика» `/analytics` (24.09.2026), пункт меню для HEAD и ADMIN: выручка выполненных по дате выполнения, средний чек, маржа с покрытием «N из M», оплаты по дате платежа, график по дням/месяцам, заказы периода по статусам со ссылками в список, топ-10 товаров. Сравнение менеджеров не делаем — решение владельца. Почему так — `docs/DECISIONS.md`, запись от 24.09 про аналитику. Домен `src/domain/analytics/` (24 теста), сервис `src/server/analytics/dashboard.ts` (4 теста по живой БД). Проверено скриптом по dev-базе: «Всё время» — 3015 выполненных на 84 275 719,71 ₽ за ~0,2 с; маржа пока «0 из N» — выполнены только архивные заказы, у них нет закупки. Пройден в браузере (расширение Claude для Chrome): все пресеты, даты руками, подсказка на графике, пустой месяц, переход из «Заказов по статусам» в список с теми же заказами
- [x] ~~XLSX~~ — не нужен, CSV хватает (решение владельца, 24.09.2026)

## M6: переписка с клиентом по почте (25.09.2026)

**Выкачено 25.09.2026** (коммит `d817419`: аналитика, груз, переписка, настройки почты): CI по `development` и `main` и деплой зелёные, `app` — healthy, `/login` — 200, миграции `20260924230000_order_cargo` и `20260925090000_client_emails` накатились. Опрос ящика в бою работает по-прежнему из `IMAP_*` окружения (`zakaz@erp.bus-com.ru`) — пока в «Настройках почты» не задан `info@bus-com.ru`.

Решения — `docs/DECISIONS.md`, запись от 25.09; требования — PRD, M6.2. Telegram-бот не делаем (владелец, 24.09).

- [x] Модели `Email` и `EmailAttachment`, события заказа `EMAIL_SENT` / `EMAIL_RECEIVED` — миграция `20260925090000_client_emails` (только новое; накатана на dev-базу и `buscom_test`, в бой уедет с выкатом)
- [x] Домен `src/domain/email/`: шаблоны с подстановками и выбросом пустых строк, разбор адресов, номер заказа из темы, цепочка Message-ID, подсказки «Сообщить клиенту?» — 12 тестов
- [x] Сервис `src/server/emails/service.ts`: отправка из заказа (SMTP → потом база с событием), счёт PDF вложением, приём входящих с привязкой по цепочке / теме / адресу клиента, ручная привязка, прочитанность — 7 тестов по живой БД (SMTP подменён)
- [x] Опрос ящика: письмо не о заказе с сайта → переписка; флаг `\Seen` на них не ставится; итог прохода считает письма
- [x] Карточка заказа — блок «Переписка»: лента, «Написать клиенту», шаблоны, «Приложить счёт PDF», подсказки после полной оплаты и трека; «Кому» по умолчанию — отправитель последнего входящего, иначе email клиента
- [x] Раздел «Почта» `/mail` (все роли, счётчик непрочитанных в меню): «Входящие», «Без заказа», «Отправленные»; письмо `/mail/[id]` с привязкой к заказу; вложения — `/api/email-attachments/[id]` (PDF и картинки во вкладке, остальное скачиванием с `sandbox`)
- [x] «Шаблоны писем клиенту» в `/admin/dictionaries`
- [x] Проверено в браузере на dev-базе двумя тестовыми входящими (потом удалены вместе с событиями): список и счётчик, прочитанность при открытии, заголовки вложения, ручная привязка к №3016, лента и форма в заказе, подстановка шаблона, редактор шаблонов. **Настоящая отправка не проверялась**: SMTP в dev-базе настроен, и письмо ушло бы наружу — первую отправку сделать в бою самим на свой адрес
- [x] **Настройки почты — отдельный раздел** `/admin/mail` (25.09.2026, просьба владельца): исходящая (SMTP, перенесена из справочников), входящая (IMAP — новое: сервер, порт, логин, пароль, кнопка «Проверить подключение») и шаблоны писем. IMAP из интерфейса главнее `IMAP_*`, читается на каждом проходе опроса — без перезапуска; курсор опроса помнит ящик (`account`), при смене ящика начинает с его конца. Проверено в браузере и тестами (домен +3)
- [x] (26.09.2026, владельцем в бою — «всё ок») **От владельца для запуска:** IMAP-доступ к `info@bus-com.ru` → «Администрирование → Настройки почты → Входящая почта» (вместо `zakaz@erp.bus-com.ru` из `.env.production`), «Проверить подключение»; там же SMTP того же ящика и «От кого» = `info@bus-com.ru`, иначе клиенты будут отвечать на другой адрес; уведомления сайта о заказах перенаправить на `info@bus-com.ru` (в OpenCart — адрес для уведомлений); реквизиты продавца заполнены — иначе счёт не приложить
- [x] **Привязка по номеру сайта и свёрнутая цитата** (25.09.2026, замечания владельца после подключения `info@bus-com.ru`): номер в теме ищется среди заказов с сайта по номеру на сайте, затем среди ручных по номеру ERP, архив не участвует; `{номер}` в шаблонах — номер для клиента. Цитата в ответах свёрнута. Тесты: домен +6, по живой БД +1; проверено в браузере на dev-базе (тестовое письмо потом удалено). В бою до выката этой правки одно письмо («Re: … - Заказ 2828») привязалось к архивному №2828 — перепривязать в «Почте» к заказу с сайта 2828
- [x] **Папки ящика** (25.09.2026, вопрос владельца: в `info@bus-com.ru` 17 805 писем, разложены по папкам в Яндексе): кнопка «Показать папки ящика» в «Настройках почты → Входящая почта» — дерево папок с числом писем, непрочитанных и суммой со вложенными, пометка «читает ERP» у «Входящих». Домен `src/domain/email/folders.ts` (+2 теста). Живьём не проверено — IMAP-доступ есть только в бою
- [x] **Импорт истории переписки за 3 года** (25.09.2026): «Настройки почты → Импорт истории переписки» — выбор папок, запуск, прогресс по папкам, «Продолжить» после сбоя. `src/server/emails/history-import.ts`, домен `src/domain/email/history.ts` (+8 тестов), сквозной тест с подменённым IMAP по живой БД. В dev проверено чтение настоящих папок Яндекса (18 папок: INBOX 17 805, Отправленные 12 824, Банк 4 702, Снабжение…, Транспортные компании 2 548); сам импорт в dev не запускался. Заодно `Order.siteNumber` (миграция `20260925120000_site_number_and_email_history`, заполнение проверено на боевой базе только чтением) и «Переписка» в карточке клиента
- [x] **Живой ящик в «Почте»** (25.09.2026, выкачено `a3f1aae`, app healthy, ошибок в логе нет): слева папки Яндекса, папка `/mail/box?folder=…`, письмо `/mail/box/letter?folder=…&uid=…`, вложения `/api/mailbox/attachment`; пометить непрочитанным, переместить, «Удалить» (в «Удалённые»), привязать к заказу. `src/server/emails/mailbox-browser.ts`; тест по живой БД на привязку. В dev проверено на настоящем `info@`: дерево папок, INBOX 17 805, открытие прочитанного письма, привязка к №3022 (потом убрана); перемещение и удаление не нажимались — они меняют настоящий ящик
- [x] **Переписка — у клиента, не у заказа** (25.09.2026, решение владельца; выкачено `11313fd` вместе со скоростью и «прочитано» живого ящика — app healthy, ошибок в логе нет): в заказе последние 10 писем переписки с клиентом с подгрузкой старых при прокрутке вверх (курсор — дата и id письма), в карточке клиента — вся; «Без заказа» → «Без клиента» с привязкой поиском; в живом ящике — «Добавить в переписку»; письма не пишут событий в журнал заказа. Тесты по живой БД переписаны (724 зелёных)
- [x] **Живой ящик: скорость и «прочитано»** (25.09.2026, замечания владельца): одно соединение IMAP на процесс с пределом 20 с на операцию, кеш списка папок и фоновый пересчёт счётчиков, «прочитано» — только действием из браузера, письмо читается из папки только на чтение. Логика проверена скриптом на настоящем письме (непрочитанное переживает перерисовку, счётчик ±1 сразу). В скрытой вкладке Chrome, которой пользуется агент, страницы «Почты» с дорисовкой не оживают (React показывает догруженные блоки по кадрам отрисовки) — у человека в видимой вкладке этого нет; проверять руками
- [x] **Запустить импорт в бою** — владельцем, 25.09.2026, «всё хорошо»
- [ ] Опрос читает только INBOX: если фильтры Яндекса раскладывают письма клиентов по папкам, они в «Почту» не попадают. Решить по дереву папок — читать ещё папки или поправить фильтры
- [x] **«Отправленные» ящика** (26.09.2026, владелец; **выкачено** `a6bfed7` — CI по `development` и `main` и деплой зелёные; DECISIONS от 26.09): письмо из ERP кладётся копией в «Отправленные» (IMAP APPEND того же текста, что ушёл по SMTP, в `after()` действия); опрос вторым проходом читает «Отправленные» (курсор `sentEmailCursor`, первый проход запоминает конец папки) и кладёт в переписку клиента — а значит, и в карточку заказа — письма, написанные в веб-почте: ответ в цепочке клиента или на адрес ровно одного клиента; номер заказа в теме не довод (письма поставщикам). Тест по живой БД +1 — прошёл в CI (локально базы `buscom_test` на этой машине нет). В живом ящике не проверено: после выката отправить письмо из заказа → оно в «Отправленных» Яндекса; написать клиенту из веб-почты → через проход опроса оно в переписке заказа

## Ответы в чате (25.09.2026)

**Выкачено 26.09.2026** вместе с `a6bfed7`. PRD, M10.8; DECISIONS от 25.09.

- [x] `ChatMessage.replyToId`, миграция `20260926000000_chat_replies` — накатана на `buscom_test` и dev-базу
- [x] «Ответить» в меню «…» любого сообщения, плашка с цитатой над полем ввода, цитата в ответе с переходом к исходному — домен +3 теста, по живой БД +1
- [ ] **В браузере не проверено**: запущенный dev-сервер отдавал 500 на ленту — держит клиент Prisma без нового поля. После перезапуска `pnpm dev` пройти: ответить, перейти по цитате, удалить исходное

## Доставка вне суммы заказа и порядок кнопок у поставщиков (25.09.2026)

**Выкачено 25.09.2026** (`8ec2b28`): CI и деплой зелёные, миграция в бою пересчитала №3024 — сумма 21 008 ₽ (товары), доставка 2 973 ₽ отдельно, запись в журнале есть.

- [x] Доставка не входит в сумму заказа (PRD, M5.2; DECISIONS от 25.09): итог, счёт PDF и статус оплаты — без неё, стоимость видна в «Доставке» и справкой под итогами; сверка «Итого» с сайта учитывает доставку. Тесты: домен +1, по живой БД +1
- [x] Миграция `20260925230000_delivery_out_of_total` (только данные) пересчитывает старые заказы с записью в журнал; накатана на `buscom_test` и dev-базу. В бою затронет один заказ (доставка 2 973 ₽, открыт, оплачен частично)
- [x] «Поставщики» в карточке заказа: «Назад» / «следующий этап» сразу под цепочкой, остальные действия ниже за чертой

## Мобильная версия (25.09.2026)

Исследование и план — `docs/MOBILE-PLAN.md`, решения — `docs/DECISIONS.md` (запись от 25.09 про мобильную версию). Все четыре этапа сделаны. **Выкачено 25.09.2026** (`b29271d`): CI и деплой зелёные, `/login`, `/sw.js`, манифест — 200.

- [x] Этап 1 — быстрые исправления: меню закрывается после перехода, страницы не шире экрана, кнопки и поля 40–44 px ниже `md`, «…» в чате на сенсорных экранах
- [x] Этап 3 — карточка заказа: блоки одним столбцом по частоте, позиции карточками, диалоги шторкой снизу, нижняя панель действий с подтверждением смены статуса, «Позвонить» / «Написать», «Поделиться» для заказа поставщику
- [x] Этап 2 — списки заказов, клиентов, товаров, поставщиков карточками; фильтры заказов свёрнуты
- [x] Этап 4 — нижнее меню, отступы под «чёлку» и полосу жестов, число непрочитанных на значке приложения, «потянуть — обновить» в режиме приложения
- [x] Проверено в Chrome (iframe 390 px на dev-базе): ни одна страница не шире экрана, порядок блоков, панели; на 1280 px вид прежний
- [ ] Пройти руками на Realme и iPhone 11 после выката: нижние панели над полосой жестов, «потянуть — обновить», число на значке, шторки с клавиатурой

## M10: чат сотрудников (25.09.2026)

Требования — PRD, M10; решения — `docs/DECISIONS.md`, запись от 25.09 про чат. **Выкачено 25.09.2026** (коммит `87e8167` вместе с пушами): CI и деплой зелёные, миграции накатились, `/login` — 200.

- [x] Модели `ChatMessage`, `ChatAttachment`, поле `User.chatReadAt` — миграция `20260925180000_staff_chat` (собрана `prisma migrate diff` из схем, туннель был закрыт). Накатана на `buscom_test` и общую dev-базу (без неё падали все страницы: меню спрашивает `chatReadAt`)
- [x] Домен `src/domain/chat/`: текст и лимиты файлов, права на правку/удаление, срок хранения, разбор «№3021»/«#3021» — 15 тестов
- [x] Сервис `src/server/chat/service.ts`: отправка с файлами, правка, мягкое удаление, лента страницами, изменения после курсора, непрочитанные, стирание файлов старше года — 7 тестов по живой БД; полный прогон с БД — 767 зелёных
- [x] Экран `/chat`, пункт меню «Чат» с живым счётчиком, маршруты `/api/chat/messages`, `/api/chat/unread`, `/api/chat-attachments/[id]`, таймер стирания в `instrumentation.ts`
- [x] (26.09.2026, владелец проверил руками — всё ок) Пройти руками двумя входами: отправка, файлы (скрепка, перетаскивание, вставка скриншота), правка/удаление, «Показать раньше», счётчик в меню у второго пользователя, ссылка на заказ

### Пуш-уведомления о сообщениях чата (25.09.2026)

Решения — `docs/DECISIONS.md`, запись от 25.09 про Web Push; требования — PRD, M10.7. **Выкачено 25.09.2026** (`87e8167`): `/sw.js`, манифест, иконки и `favicon.ico` в бою отдаются 200 без входа; образ с `public/` собрался.

- [x] Модель `PushSubscription` — миграция `20260925200000_push_subscriptions`, накатана на dev-базу и `buscom_test`
- [x] Домен `src/domain/push/payload.ts`: проверка подписки (https, только сервисы пушей), текст уведомления — тесты
- [x] Сервис `src/server/push/service.ts` (ключи VAPID в `Setting`, отправка, чистка отвергнутых подписок) — 5 тестов по живой БД с подменённым `web-push`; пуш из чата — `src/server/chat/push.ts` через `after`
- [x] PWA: `src/app/manifest.ts`, `/sw.js`, иконки `public/icons`, `appleWebApp` в корневом layout, `public/` в Dockerfile, исключения в `src/proxy.ts`
- [x] «Настройки → Уведомления на телефоне»: включить, проверить, выключить; подсказки для iPhone в Safari, запрета в браузере, http
- [x] Проверено без входа: `/sw.js`, манифест и иконки отдаются 200 без cookie, синтаксис воркера верный
- [ ] **В браузере и на телефонах не проверено** (расширение браузера не было подключено; телефоны достают только до боя по https). После выката: Realme (Chrome) — «Настройки → Включить → Проверить»; iPhone 14 — Safari → «На экран Домой», войти из значка, включить; затем сообщение в чате со второго устройства
- [x] Образ с `public/` собрался в CI, выкат прошёл (11 мин)

## Что в этапе 1 осталось незакрытым

- [x] **Файлы заказа крупнее ~6 МБ не загружались** (найдено и исправлено 25.09.2026): накладная и счёт поставщика обещают до 15 МБ (`MAX_DOCUMENT_BYTES`), а лимит тела Server Action был 6 МБ. Теперь `bodySizeLimit` и `proxyClientMaxBodySize` (proxy.ts буферизует тело, по умолчанию 10 МБ) — 16 МБ. Заодно: `delete` в Prisma возвращал удалённую строку вместе с байтами файла — удаление 12 МБ через туннель падало по таймауту транзакции 5 с (P2028); теперь `select: { id: true }` (файлы заказа, счёт поставщика, картинки товара), у загрузки таймаут 30 с. Проверено на dev (№3016): 12 МБ загрузился, 15,5 МБ — «Файл больше 15 МБ», удаление 12 МБ — 1 с
- [x] Интеграционные тесты прогоняются локально по базе `buscom_test`: на 21.09.2026 — **310 тестов из 29 файлов** зелёные. Миграции накатились на пустую базу с нуля — заодно проверены и они. Команда: `RUN_DB_TESTS=1 DATABASE_URL=postgresql://buscom:buscom@localhost:5432/buscom_test pnpm test`
- [x] Все экраны пройдены в браузере руками (2026-09-20, расширение Claude для Chrome). Что нажималось и что сошлось — ниже, в «Проход по экранам»
- [x] Экран 403 вместо стандартной ошибки Next: `requirePageUser` на страницах зовёт `forbidden()`, UI — `src/app/(app)/forbidden.tsx` внутри меню и шапки. Матрица прав прогнана на живой БД (тогда ролей было четыре, сейчас три — складскую убрали, см. DECISIONS)
- [x] (24.09.2026, владельцем в «Справочники и настройки → Почта» в бою) Заполнить SMTP-переменные перед запуском, иначе письма со сбросом пароля только в логе
- [x] (в бою — 24.09.2026, владельцем) Убрать демо-данные перед боевым запуском. В базе разработки их уже не отделить на глаз: рядом лежат 4306 настоящих клиентов и 3017 архивных заказов. Демо-заказы после импорта имеют номера **3018–3031**, демо-клиенты — те шестеро, у кого эти заказы; демо-товаров 6

## Проход по экранам в браузере (2026-09-20)

> Запись сделана до удаления складского блока: всё про склад, резерв, остаток, вид «К отгрузке» и статус «Сборка» здесь устарело. Остальное в силе.

Прокликано руками под всеми четырьмя ролями (роль своей учётки переключалась в dev-БД и возвращена в ADMIN).

Сошлось с PRD:

- Список: колонки, вкладки со счётчиками, множественный выбор статуса, «Сбросить», всё состояние в URL (`?status=…&status=…&view=mine&q=…`). Поиск `89031234455` находит клиента с телефоном `+7 (903) 123-44-55`. Просроченные подсвечены розовым, у них «5 раб. дн.» в колонке «В статусе». Склад по умолчанию открывает «К отгрузке» и не видит пункта «Администрирование».
- Карточка: кнопки ровно по `availableTransitions`; полная оплата сама переводит «Ждёт оплаты» → «Оплачен» с автором «система» в ленте; диалог отмены требует причину из справочника (7 позиций) и даёт свободное уточнение; подбор товара по части названия; «Взять себе» на заказе с сайта назначает менеджера и переводит в «В работе» одним действием.
- Лимит скидки: менеджеру при скидке 20 000 ₽ от 73 500 ₽ выводится «Скидка превышает лимит 10% (7 350 ₽) — сохранить сможет только руководитель», сервер сохранение отклоняет (в БД скидка осталась 0, события не появилось).
- Отгрузка ТК без трек-номера отклоняется: «Для отгрузки транспортной компанией нужен трек-номер», статус не меняется.
- Склад видит карточку без правки состава, без формы оплаты и с заблокированным полем «Стоимость» в доставке; трек-номер ему доступен.
- Экран 403 выглядит как задумано: меню и шапка на месте, кнопка «К заказам».
- PDF: счёт 21 КБ и комплектовочный лист 19 КБ, `application/pdf`, `inline; filename="schet-9.pdf"`; без сессии — редирект.
- Товары: «Свободно −2» у позиции с резервом больше остатка подсвечено красным; товар «под заказ» показывает «—» и не резервируется.

Найдено и исправлено:

- **Форма оплаты держала прежний остаток.** `useState` брал остаток один раз, и после платежа в поле оставалась сумма прошлого остатка: после полной оплаты там висело 5 933,34 ₽, а `addPayment` переплату не проверяет — повторный клик молча записал бы лишний платёж. Теперь поле подхватывает новый остаток (правка состояния при смене пропсов в рендере, не в эффекте — `react-hooks/set-state-in-effect`). Проверено: 73 500 → платёж 10 000 → в поле 63 500 → платёж → в поле 0.00.

Осталось замечанием, не чинилось:

- ~~**В списке кликабелен только номер заказа**~~ — исправлено 2026-09-20: строка кликается целиком (`src/components/orders/order-row-link.tsx`). Ссылка на номере осталась для клавиатуры и «открыть в новой вкладке»; клик по вложенной ссылке или кнопке и выделение текста переходом не считаются. Проверено в браузере: клик по ячейке с датой открывает карточку.
- **Сервер не отвергает переплату** (`addPayment` проверяет только «больше нуля»). Случайный путь закрыт формой. Похоже, так и надо: PRD в «Бизнес-правилах» перечисляет «Переплата» среди статусов оплаты, и он реализован (`PAYMENT_STATUS_LABELS`). То есть запрещать её значило бы противоречить PRD; остаётся спросить заказчика, нужно ли предупреждение в интерфейсе.
- **Глазами не проверены**: блокировка входа после 10 неудачных попыток и переназначение менеджера руководителем (оба покрыты серверной логикой и тестами).
- **Демо-данные сдвинулись**: заказ №9 стал «Оплачен», №11 полностью оплачен, №15 переведён в «Сборка» с назначенным менеджером. Всё равно удаляются перед боевым запуском.

## Заведение клиента из интерфейса (2026-09-20)

Требование владельца, дописано в PRD (M2.4 и критерии приёмки «Клиенты `/customers`»): клиента нужно уметь завести до первого заказа — позвонили, договорились, заказа ещё нет. Раньше клиент появлялся только как побочный эффект заказа (`findOrCreateCustomer` из `orders/create.ts` и приёма заказа с сайта).

Сделано:

- `createCustomer` в `server/customers/service.ts`: права через тот же `canEditCustomers`, что и правка карточки, нормализация телефона, всё в транзакции;
- **дубль не заводится**: перед вставкой идёт `findCustomer` из `customers/match.ts` — то же правило, что при приёме заказа (сначала телефон, потом email). Найденный клиент возвращается не молча, а исключением `CustomerExistsError` с его id и именем: человек нажал «Новый клиент» и должен увидеть, что такой уже есть;
- Server Action `createCustomerAction` отдаёт `existingId`, форма показывает ссылку «Открыть его карточку»;
- UI: `components/customers/new-customer-dialog.tsx` — кнопка «Новый клиент» в шапке списка, короткая форма (тип, имя, телефон, email, ИНН/КПП для юрлица, комментарий), после создания уводит в карточку;
- **адреса доставки заводятся в той же форме** — сколько нужно, один помечается «по умолчанию» (без пометки им становится первый, пустые строки отбрасываются). Клиента не с сайта заводят целиком и сразу, вторым заходом в карточку ходить не нужно;
- 7 тестов в `server/customers/service.db.test.ts`: нормализация, дубль по телефону в другом формате, дубль по email в другом регистре, однофамильцы без контактов заводятся оба, пустое имя отклоняется, адреса с пометкой и без.

Прогон в браузере: «Сидорова И. П.» с телефоном ИП Сидоровой — тост «Клиент «ИП Сидорова» с таким телефоном или email уже есть» и ссылка на её карточку, второй записи не появилось; с другим номером клиент завёлся, телефон в базе `+79267771122`, открылась его карточка. Отдельно проверены адреса: ООО «Транс-Сервис» с двумя адресами, пометка «по умолчанию» при отметке второго снялась с первого, в карточке оба адреса и правильный по умолчанию. Тестовые записи из локальной базы удалены.

Полный прогон по живой БД зелёный: `RUN_DB_TESTS=1 DATABASE_URL=…/buscom_test pnpm test` — 254 теста в 24 файлах. Миграция удаления склада накатана и на `buscom_test`.

### Выгрузка клиентов из прежней ERP (2026-09-21)

Файл `misc/clientbase.csv` — 4424 клиента, 44 колонки, cp1251, разделитель `;`. **Папка `misc` добавлена в `.gitignore`**: там персональные данные (телефоны, паспорта), в репозиторий им нельзя. Раньше `.gitignore` содержал только комментарий `# misc` — правила не было, и `git add -A` утащил бы файл целиком.

Разобранная структура и решения владельца (что оставляем):

- **пустые во всех строках — выброшены** (4): «История „Онлайн Консультанта“», «Группа доступа», «Логин», «Пользователь»;
- **паспортные данные — одно поле** вместо шести. Серия (496), номер (489), дата выдачи (118), кем выдан (98), адрес прописки (100) склеены в `Паспорт` (504 строки); готовая строка старой ERP не годилась — в ней текст вида «Дата выдачи не указанаКем выдан - не указано». День рождения (34) выброшен;
- **реквизиты юрлица — всё, кроме ОКАТО (195), ОКПО (443) и лицевого счёта** (19, из них 11 — не счёт, а название банка). Остаются: юр. название, юр. адрес, ОГРН, ИНН, КПП, банк, БИК, р/с, к/с, руководитель, бухгалтер. 903 строки хоть с одним реквизитом, 678 — с полным банковским набором;
- **воронка старой CRM («Тип», «Статус») — в комментарий клиента**, отдельных полей в схеме не заводим;
- **служебное выброшено**: «Время в статусах» (PHP-сериализация), «Лог рассылки», «Дата следующего контакта», «Период отказа», «Фильтр для конт.лица», «Наша компания», «Менеджер», «Сайт», «Деятельность».

Итог — `misc/clientbase-clean.csv`, 22 колонки (UTF-8 с BOM).

**Решения владельца по структуре (2026-09-21) и что по ним сделано:**

- **имя клиента** — у физлица ФИО, у юрлица «Название». В выгрузке эти колонки совпадают у 4304 строк из 4424, а где расходятся, у юрлиц в «ФИО» лежит имя контакта или город;
- **телефон** — может быть пустым, но непустые уникальны. `Customer.phone` получил `@unique` (в Postgres UNIQUE не мешает нескольким NULL). Это же стало ключом повторного прогона импорта;
- **из ячейки берём первый номер**, остальные уходят в комментарий: 235 ячеек содержали больше одного;
- **email** — первый, остальные в комментарий; одинаковый email у разных клиентов разрешён. Отсюда правка сопоставления: по email клиент находится, только если email ровно у одного, иначе общая почта бухгалтерии привязала бы заказ к случайному юрлицу;
- **адрес одной строкой** и в ERP: `CustomerAddress.city` убран, города миграция приклеила к адресу;
- **паспорт** — одно поле, собирается из пяти колонок выгрузки;
- **реквизиты юрлица** — в `Customer.requisites` (Json), форма задана `customerRequisitesSchema`;
- **контактное лицо** — своя колонка;
- **«Тип» и «Статус» прежней CRM** дописываются в комментарий клиента.

Миграция `20260921100000_customer_import_fields` — руками: генератор убрал бы `city` без переноса данных, а уникальный индекс по телефону упал бы на дублях без внятного текста. В миграции стоит проверка, которая называет задвоенные номера и советует слить их на `/customers`.

**Импорт** — `pnpm import:customers misc/clientbase.csv [--dry-run] [--only-keyed]`. Скрипт читает исходный файл прежней ERP как есть (cp1251, `;`), готовить его вручную не нужно. Прогон на тестовой базе с нуля: создано 4306, дополнено 91, без изменений 27, слито внутри файла 118, без телефона и ИНН 315. В базе после этого: 4306 клиентов (923 юрлица), 3952 с телефоном, 874 с реквизитами, 518 с контактным лицом, 504 с паспортом, 2927 адресов. **Повторный прогон создал 0 и не изменил ничего** — идемпотентность проверена.

**База клиентов залита и в базу разработки** (`buscom_erp`, 21.09.2026, по просьбе владельца — «пускай они будут тут всегда»). Стало 4312 клиентов: 4306 импортированных плюс 6 демонстрационных, у которых остались их 14 заказов. ИП Сидорова совпала с выгрузкой по телефону и была дозаполнена, а не задвоена. Учитывать это дальше: база разработки больше не чистый демо-стенд, в ней настоящие персональные данные — ронять её `migrate reset`-ом нельзя, и в выгрузках с этой машины она тоже настоящая.

**Удаление клиента** (21.09.2026): кнопка в карточке, подтверждение диалогом, право у руководителя и администратора (`CUSTOMER_DELETE_ROLES`). Клиента с заказами сервер не удаляет — предлагает объединить с основным; учитываются и удалённые заказы, потому что строки в базе они занимают. Удаление физическое, а не `deletedAt`: телефон уникален, и помеченная удалённой запись навсегда заняла бы номер (см. `docs/DECISIONS.md`). Проверено в браузере на обоих случаях.

**Мусор в именах, замеченный после заливки** (правится руками в карточках, в импорте чинить нечего — так записано в источнике): 15 клиентов с именем из одних цифр или даты («1», «02052025», «11.01.2021»), 10 с именем в один-два символа, 1 email вида `empty1568622190@localhost.net`.

**Что осталось по импорту:**

- 315 строк без телефона и ИНН заводятся, но повторный прогон их задвоит. Если импорт придётся гонять несколько раз — второй и последующие прогоны делать с `--only-keyed`;
- боевой прогон ещё не делался: сначала выкат миграции, потом импорт на сервере;
- дубли **внутри самой выгрузки** (118 строк) слиты по ключу автоматически; дубли, различающиеся и телефоном, и ИНН, останутся отдельными клиентами — их сливают руками на `/customers`.

**Прежний список вопросов (закрыт):**

- **ключа записи нет** ни в одном столбце. Повторный прогон импорта наплодит дублей; у клиента в схеме нет ничего вроде `(source, externalId)`, как у заказов. Скорее всего нужна миграция;
- **«ФИО» и «Название» совпадают в 4304 строках из 4424**, в остальных 120 это разное: у юрлиц в «ФИО» лежит имя контакта или город («Ярославль» при названии «ООО „Светлогорскавто“»). Какое поле считать именем клиента — решать;
- **телефоны**: 235 ячеек содержат больше одного номера («факс (840153) 22054, тел. (840153) 22052»), 284 пустых, нормализуются 3549 из 4424. У клиента в ERP телефон один, и именно по нему ловится дубль;
- **email**: 277 ячеек с несколькими адресами;
- **дубли внутри самой выгрузки**: 160 строк с повторяющимся телефоном, 169 — с повторяющимся email;
- **колонки загрязнены**: в «Адрес» попадаются телефоны, в «Адрес прописки» — текст заявки с сайта, в «Лицевой счет» — названия банков;
- **«Адрес» одной строкой** (2942 заполнено), города отдельно нет — в ERP адрес разбит на город и адрес;
- **ИНН**: 3 значения с пробелами («662 304 9453»); у 209 юрлиц ИНН нет вовсе, у одного физлица — есть.

**Дальше по клиентам.** Согласованный состав полей ложится на модель так: реквизиты юрлица — в `Customer.requisites` (Json, поле в схеме есть и до сих пор не использовалось), паспорт — новое поле, контактное лицо (532 строки) — новое поле, «Тип» и «Статус» — дописываются в `comment`. Сам импорт — скрипт или экран загрузки с отчётом «создано / обновлено / пропущено как дубль». Прежде чем писать, нужно закрыть вопросы из списка выше.

**Замечено попутно:** `canEditCustomers` сейчас пропускает все три роли — складская была единственной, кому правка клиентов не полагалась. Проверка оставлена как контракт серверного слоя, но ветка `ForbiddenError` недостижима, поэтому теста на 403 для клиентов нет. Появится роль без права правки — тест писать тогда.

## Правка позиции и карточка товара из заказа (2026-09-22)

В строке позиции карточки заказа две новые кнопки:

- **«Изменить позицию»** — окно правки этой позиции: перевыбор опций товара (цена пересчитывается по каталогу — базовая + надбавки, её можно поправить руками), цена, количество, скидка. Меняет только заказ; в базу — «Сохранить состав». Выбор опций вынесен в общий `OptionGroupSelects` / `useOptionChoice` (`product-options-chooser.tsx`) — тот же, что при добавлении позиции;
- **«Карточка товара»** — тот же диалог, что в «Товарах» (цена, категория, поставщики, опции, картинка), для правщиков каталога. После сохранения страницы заказов перечитываются (`revalidatePath("/orders/[number]", "page")`), и поставщики у позиций берутся из свежих данных товара, а не из снимка на момент открытия.

Проверено в браузере на заказе №3018 (правки отменены, в базу не писалось): добавлено «Стекла на Ford Transit LWB» с вариантом за 4 750 ₽, «Изменить позицию» → вариант с форточкой → цена 12 250 ₽; «Карточка товара» у LK-700 открывает диалог товара.

**Ограничение:** у позиции, добавленной и ещё не сохранённой, кнопки «Карточка товара» нет — данные товаров страница загружает при открытии; появится после «Сохранить состав». Опции такой позиции править можно — они берутся из подбора товара.

## Справочник категорий (M3, 2026-09-22)

Категория товара — ссылка на справочник-дерево; решение — `docs/DECISIONS.md`, «Справочник категорий».

- миграция `20260922120000_product_categories` (руками: перенос строк в справочник, потом удаление колонки) накатана на `buscom_erp`: 26 категорий, у 203 товаров категория сохранена;
- домен `src/domain/product/categories.ts` (+ 7 тестов): дерево, путь, потомки, проверка переноса и названия; сервер `products/categories.ts` (+ 4 теста по БД); экран `/products/categories`; выбор категории деревом в фильтре и карточке товара, путь «Раздел / Подкатегория» в таблице и выгрузке CSV;
- каталог сайта перевыгружен с путями категорий и разложен: 4 раздела, 25 подкатегорий; повторный прогон — 215 «без изменений»;
- **в бою с 22.09** (выкат `6f77a21`): миграции картинок и категорий применились; на сервере `import:site-products` разложил каталог по дереву (26 обновлено), `import:site-images` загрузил 215 аватарок прямо с bus-com.ru. Проверено в боевой админке: дерево на `/products/categories`, путь «Раздел / Подкатегория» и превью в списке товаров. В локальном `pnpm dev` экран не открывается до перезапуска — старый Prisma-клиент.

## Новый сайт bus-com.ru на общей базе (2026-09-24)

Владелец попросил переписать сайт в текущей инфраструктуре, на одной базе с ERP, с редизайном и упором на SEO. Требования — `docs/SITE-PRD.md`, план по этапам — `docs/SITE-PLAN.md`.

**26.09.2026 — начат этап 2 (снимок старого сайта).** Сервер поднят до 4 ГБ / 2 ядер / 20 ГБ; владелец ответил на вопросы этапа 0 (записаны в SITE-PRD, «Открытые вопросы»): Метрика 36097845, DNS у Джино, наличия не будет, калькулятор и производители — не в MVP, переоборудование (страница и категория) убирается.

- [x] Снимок SEO старого сайта в репозитории: `docs/site-snapshot/sitemap.xml` (эталон, 699 адресов) и `pages.json` (727: карта + главная + 27 канонических адресов категорий, которых в карте нет). Скрипт `pnpm snapshot:old-site`, разбор `src/domain/site/old-site.ts` + 7 тестов. Все адреса ответили 200
- Что показал снимок: **товаров 215** (не 207), у каждого один canonical; **у 14 товаров ЧПУ нет** — canonical `index.php?…product_id=N` (238, 350, 351, 357, 367, 370–373, 422, 425, 426, 470, 472), им нужны новые слуги и 301; **категорий 33**, canonical у всех короткий (`/polki`), пути с разделом — дубли; у 197 из 215 товаров title написан руками (≠ h1); `/paz` — категория, не производитель; в категории переоборудования товаров нет
- [x] Миграция `20260926120000_site_seo`: `slug`, `metaTitle`, `metaDescription` у товара и категории, таблица `UrlRedirect` (DECISIONS от 26.09). Собрана `prisma migrate diff` (теневую базу роль `buscom_dev` создать не может), накатана на dev-базу; в бой уедет с выкатом
- [x] Импорт `pnpm import:site-seo` (`--dry-run` — без записи): план в `src/domain/site/seo-import.ts` + `slug.ts`, 14 тестов. **В dev-базе:** 215 товаров и 28 категорий со слугом и метатегами, 494 переадресации (453 на товар, 26 на категорию, 14 на главную, 1 — 410). Сверка: 201 канонический адрес товара + 28 категорий + 494 + 4 остающиеся страницы = 727 адресов снимка. Повторный прогон ничего не меняет
- [x] Убранные адреса — 301 на главную (владелец, 26.09); домен остаётся bus-com.ru, «переключение» — смена DNS-записи
- [ ] **В бою слугов и переадресаций ещё нет**: после выката миграции прогнать `pnpm import:site-seo` по боевой базе (как — решить: туннель к боевой базе или SQL-файл, как `misc/prod-import.sql`). Срочности нет — сайта ещё нет
- [ ] `/paz` → на главную временно; на этапе 7 — на страницу модели ПАЗ
- [ ] В ERP нет экрана для слуга и метатегов — нужен до запуска сайта (смена слуга руками — только с переадресацией)
      **26.09.2026 — этап 1, шаг 1: ERP переехала в `apps/erp`** (решение владельца — полный монорепозиторий, как в плане). Корень — рабочее пространство pnpm (`apps/*`, `packages/*`), команды — в CLAUDE.md. Код ERP не менялся; правки — пути: `next.config.ts` (`outputFileTracingRoot` и `turbopack.root` — корень, пути Tesseract от корневого `node_modules/.pnpm`), Dockerfile (контекст — корень, сервер в `apps/erp/server.js`, ссылка на модель Tesseract), CI (новая задача `docker` собирает оба образа на каждом пуше — Dockerfile проверяется до выката). Локально: `pnpm check` зелёный, `next build` собрал standalone с Tesseract и pdfmake, dev-сервер отдаёт `/login`.

- **На второй машине после `git pull`:** перенести `.env` из корня в `apps/erp/.env`, `misc/` — в `apps/erp/misc/`, затем `pnpm install`
- [x] Шаг 1 в CI: образы собрались, проверка OCR в образе прошла; упал только `format:check` (STATUS не отформатирован) — починено в шаге 2
- [x] **Шаг 2 — `packages/db`**: схема, миграции, `prisma.config.ts` и сгенерированный клиент; импорты ERP `@/generated/prisma/*` → `@buscom/db/client`, `@buscom/db/enums`. Сид остался в ERP (`apps/erp/scripts/seed.ts`, `pnpm erp db:seed`) — он заводит администратора через better-auth. Команды Prisma — из корня: `pnpm db:migrate`, `db:deploy`, `db:generate`, `db:studio`. `.env` Prisma берёт из окружения, иначе из `apps/erp/.env`
- [x] **Шаг 3 — `packages/domain`**: `src/domain` целиком, импорты `@buscom/domain/<путь>`, внутри пакета — относительные; свои `tsconfig` и `vitest.config` (586 тестов; в ERP осталось 25 + 190 по живой БД). Фикстуры писем и страниц — в `.prettierignore` по новым путям
- [x] **Шаг 4 — каркас сайта `apps/site`** (порт 3001 в dev): шапка, подвал, шрифты Golos Text и IBM Plex Mono, палитра макета токенами Tailwind, главная с метатегами старого сайта, `yandex-verification`, `robots.txt` и `sitemap.xml` (пока только главная), 404. Закрыт от индексации переменной `SITE_INDEXING` (DECISIONS от 26.09). Образ `site` в Dockerfile, сервис `site` в compose (`SITE_PORT`, по умолчанию 82 — свободен на сервере), выкат пушит и проверяет его. Локально проверено `next start`: 200, `X-Robots-Tag: noindex`, robots запрещает всё, запросов к Google Fonts нет. Standalone на Windows не запускается (симлинки pnpm) — в образе Linux это не мешает
- [ ] **Временный адрес сайта**: владельцу — в панели Джино спроксировать поддомен (например, `new.bus-com.ru`) на порт 82 (docs/DEPLOY.md, «Сайт bus-com.ru»)
- [x] **Этап 1 выкачен 26.09.2026** (`6c6aad4`): CI и деплой зелёные. Проверено в бою: `erp.bus-com.ru/login` — 200, контейнеры `app`, `site`, `postgres` — healthy, сайт на 127.0.0.1:82 — 200 с `X-Robots-Tag: noindex`, robots.txt запрещает всё; миграция `20260926120000_site_seo` накатилась; память — 606 МБ из 4 ГБ
- Дальше — этап 3 (картинки в хранилище) и этап 4 (каталог); импорт слугов в боевую базу (`pnpm erp import:site-seo`) — до запуска сайта
- [x] **Временный адрес сайта** `https://new.bus-com.ru` → порт 82 (владелец, 26.09): 200, `noindex`. `http://` не переводит на https — включить перенаправление в панели Джино

**26.09.2026 — этап 4, первый кусок: каталог на сайте** (DECISIONS от 26.09 про каталог сайта). **Выкачено 26.09.2026** (`8c7224c`): CI и деплой зелёные, `https://new.bus-com.ru` показывает каталог.

- [x] `/{slug}` — товар или категория; главная — разделы каталога, строка меню каталога под шапкой; `sitemap.xml` из базы (244 адреса на dev); картинки `/img/{id}` из базы
- [x] Карточка: галерея, код товара, конфигуратор опций с живой ценой, «Подходит для», описание, похожие товары, разметка `Product` и `BreadcrumbList`; метатеги — из ERP (перенесены со старого сайта), иначе шаблон «{название} — купить в Нижнем Новгороде | Баском»
- [x] Категория: крошки, подкатегории, карточки товаров с ценой «от», `ItemList`, блок «много мест на автопарк»
- [x] Переадресации старых адресов в `proxy.ts` (301/410); проверено на dev: `/detali-salona/polki` → `/polki`, `/russia/klei…` → товар, `index.php?…product_id=470` → новый слуг, `/webasto` → `/`, `/kondicionery` → «Климат», `/refubrishment_test` — 410, неизвестный адрес — 404
- [x] **Импорт слугов и переадресаций в боевую базу** (26.09, с согласия владельца): сначала `--dry-run`, потом запись — те же цифры, что на dev (215 товаров, 28 категорий, 494 переадресации). Запуск на сервере: `docker compose … run --rm --no-deps -v /opt/buscom-erp/docs/site-snapshot:/snap:ro migrate pnpm import:site-seo /snap/pages.json`. Без `MIGRATE_IMAGE` compose собирает образ миграций на сервере (деплой свой удаляет) — после запуска `docker image rm buscom-erp-migrate:local` и `docker builder prune -af`
- [x] **Обход всех 727 адресов снимка на `new.bus-com.ru`**: 230 — 200, 493 — 301 на живую страницу, 1 — 410 (`/refubrishment_test`), **3 — 404: `/kontakty`, `/oplata-dostavka`, `/privacy`** — статические страницы, этап 6 (тексты есть в снимке)
- [x] **Статические страницы** `/kontakty`, `/oplata-dostavka`, `/privacy` (26.09, в `development`): тексты — со старого сайта (метатеги дословно), в коде; редактирование из ERP — этап 6. Все 727 адресов снимка теперь живые
- [ ] **`/privacy` — черновик политики обработки ПДн (152-ФЗ) на ООО «Баском», проверить владельцу или юристу до запуска.** Старый текст по этому адресу скопирован с сайта барбершопа (`barbershopnn.ru`) — не переносится
- [ ] «Доставка и оплата»: владельцу проверить актуальность текста (оплата «по карте Сбербанка», НДС 20%, перевозчики)
- [ ] **http → https на Джино отдаёт 302, а не 301** (проверено на `new.bus-com.ru`). Для `bus-com.ru` в день переключения нужен 301 — найти настройку в панели или переадресовывать в приложении
- [ ] Не сделано из этапа 4: вёрстка по макету (мега-меню, хиты, подбор по модели, баннер цеха, фильтры и сортировка в категории), признак «Хит» в ERP, `next/image` (после этапа 3), замер LCP
- [ ] Отдельная роль базы для сайта — до запуска

**26.09.2026 — этап 5: корзина и оформление заказа** (DECISIONS от 26.09 про корзину сайта). Не выкачено, в `development`.

- [x] «В корзину» в карточке (количество, выбранные опции), счётчик в шапке, `/korzina`: позиции с пересчётом по базе, количество, удаление; оформление в три шага (покупатель — частное лицо или организация с ИНН/КПП; доставка — ТК из справочника или самовывоз; оплата и комментарий), согласие с политикой ПДн, поле-ловушка; экран «Заказ № … принят» с номером ERP
- [x] Заказ уходит в ERP по API с HMAC; контракт v1 расширен совместимо: `items[].options`, `numberedByErp`. Тесты: домен +7 (корзина, форма, сборка заказа проходит контракт), ERP по живой БД +2 (опции снимком, номер без siteNumber) — прогонит CI
- [x] Проверено на машине разработки через заглушку ERP (подпись и контракт): пересчёт по dev-базе (4 970 ₽ + ремень 700 ₽ = 5 670 ₽ за штуку), выпавшая позиция блокирует отправку, ошибки формы все сразу, заказ с опциями принят. **В браузере не проверено** — расширение Chrome не подключилось
- [x] Найдено по пути: у 12 вариантов опций (3 товара) в названии был `<img>` превью со старого сайта — разбор теперь теги вырезает, миграция `20260926150000_option_value_names_without_html` чистит сохранённые (на dev накатана; снимков в заказах с HTML нет)
- [x] Починено: корневой `pnpm db:deploy` вызывал встроенную команду pnpm `deploy`, а не миграции — скрипты пакета `@buscom/db` переименованы (`migrate:dev`, `migrate:deploy`), корень зовёт их через `run`
- [ ] Письмо покупателю с составом заказа (SITE-PRD: «покупателю уходит письмо») — ERP пока уведомляет только сотрудников
- [ ] Ограничение частоты заказов по IP (сейчас только поле-ловушка)
- [ ] Реквизиты по ИНН автозаполнением (в ERP есть `company-lookup`), «Купить в 1 клик», «отправить корзину в Max»
- [ ] **При переключении DNS — выключить приём заказов из писем** (SITE-PRD, «Заказ с сайта», п. 6)

Что выяснил аудитом живого сайта (24.09):

- в `sitemap.xml` **699 адресов, но всего 207 товаров** — на товар приходится по три адреса (`/slug`, `/раздел/подкатегория/slug`, `/russia/slug`) плюс 14 штук `index.php?route=product/product&product_id=N`. Канонический адрес уже короткий — его и берём;
- `title` и `description` **написаны руками** и не выводятся из названия («Клей для ткани в салон микроавтобуса купить» против h1 «Клей для ткани 1 кг»). Их надо снять с живого сайта до переключения — потом будет неоткуда;
- **`robots.txt` на сайте нет** — отдаёт страницу «не найдено»; нет `og:`, нет canonical на главной, нет подтверждения Google. Есть Метрика и `yandex-verification`, его нужно перенести;
- в базе ERP не хватает: слуга, метатегов, производителя, моделей авто как справочника, признака наличия, «хитов», услуг цеха, статических страниц, таблицы переадресаций.

Два решения, которые стоит проговорить с владельцем до начала:

- ~~**память сервера**~~ — **решено 26.09.2026**: владелец поднял тариф до 4 ГБ, 2 ядер и 20 ГБ SSD (проверено по SSH: занято 571 МБ памяти и 5,5 ГБ диска). Было: 1,5 ГБ, занято 478 МБ. Второй процесс Node под публичным трафиком — ещё 200–400 МБ, swap на OpenVZ не добавить. Нужен тариф от 4 ГБ **до** выката;
- **картинки в объектное хранилище**: для внутренней админки решение «файлы в базе» остаётся верным, но публичный сайт с 910 картинками на слабой машине упрётся в скорость. Это меняет запись от 24.09 «в хранилище пока не переносим» — причина другая, публичный трафик.

Макеты разобраны: 7 экранов (главная, категория, карточка, корзина с оформлением, переоборудование с калькулятором, доставка и оплата, контакты), каждый в двух размерах. Шрифты Golos Text и IBM Plex Mono, зелёный `#008244` и оранжевый `#f39200`. В макете есть новое против текущего сайта: подбор по модели авто, мега-меню, конфигуратор сиденья с живой ценой, расчёт комплекта на салон, калькулятор работ цеха, связь через Max, фильтр «только в наличии».

**Расхождение в макете**: экран оформления обещает «защищённую страницу оплаты», а экран доставки — «ссылку на оплату пришлёт менеджер». По решению владельца эквайринга в объёме нет, значит текст на экране оформления переписываем.

## Бэкапы в бою заведены (2026-09-24)

До этого дня выгрузка не делалась ни разу: `/backups` был пуст, расписания не было. Теперь есть, и картинки в ежедневный дамп не попадают — `docs/DECISIONS.md`, «Бэкап без байтов картинок».

- `scripts/backup-db.sh` — два режима: ежедневный дамп с `--exclude-table-data='"ProductImage"'` (хранение 30 дней) и `--images` с `--data-only --table='"ProductImage"'` (хранение 14 дней);
- crontab пользователя `deploy`: ежедневно 03:30 и по воскресеньям 04:30, лог `/home/deploy/buscom-backup.log`. Вызов через `sh scripts/...` — бит исполнения в git не хранится, и `git reset --hard` при выкате сбрасывал бы `chmod +x`;
- **проверено на живом сервере**: ежедневный файл **1,2 МБ**, выгрузка картинок **322 МБ**. В ежедневном есть `CREATE TABLE "ProductImage"` и нет её строк, в выгрузке картинок наоборот; заказов в дампе 3021 — ровно столько же, сколько в базе. Прогон в урезанном окружении (`env -i PATH=/usr/bin:/bin`) проходит, то есть из cron заведётся;
- диск сервера: 2,2 ГБ свободно. В установившемся режиме бэкапы займут около 680 МБ (30 × 1,2 МБ + 2 × 322 МБ).

**Копия вне сервера сделана 24.09.2026** — на машине владельца: `scripts/pull-backups.ps1` + задание Планировщика «BusCom ERP — копия бэкапов» (ежедневно в 10:00, пропущенный запуск — после включения). Первый прогон скачал 4 файла (3 × 1,1 МБ и 321 МБ картинок), `gzip -t` — все целые, дамп дописан до конца; повторный прогон ничего не качает; запуск из Планировщика — код 0; недоступный сервер — запись «ОШИБКА» в лог и код 1. Подробности — `docs/DEPLOY.md`, «Бэкапы».

**Восстановление проверено 24.09.2026** — требование PRD «до запуска». Скачанные `buscom_2026-09-24_0330.sql.gz` и `buscom-images_2026-09-24_0219.sql.gz` залиты по порядку в локальный Postgres 16 (схема `restore_test` в `buscom_erp`: у роли `buscom` нет `CREATEDB`, отдельную базу не завести; `public.` в DDL заменён awk-ом, строки данных COPY не тронуты) с `ON_ERROR_STOP=1` — без единой ошибки, внешние ключи и индексы создались после данных. Все 26 таблиц: число строк совпало с числом строк в дампе (заказов 3021, клиентов 4337, картинок 910 / 283 МБ, пустых нет). Кириллица читается, сумма заказов 84 344 674,71 ₽. Схема после проверки удалена.

**Замечено при проверке: боевая база в кодировке `SQL_ASCII` с сортировкой `C`** (`pg_database`, проверено по SSH). Данные — корректный UTF-8: весь дамп лёг в UTF-8 базу без ошибок. Но сама база кодировку не проверяет, а `lower()`/`upper()`/`ILIKE` не знают кириллицы и сортировка по названию — побайтная. Исправляется только пересозданием базы в UTF8 с восстановлением из дампа — отдельная работа с простоем, до неё не трогать.

## Выкат 24.09.2026 (коммит `39fc64c`)

Влито в `main`, CI зелёный, деплой прошёл сам: образы собраны, миграция `20260924010000_product_description` накатилась контейнером `migrate`, приложение поднялось (`healthy`).

- **Запрет индексации в бою работает**: `curl -I https://erp.bus-com.ru/login` отдаёт `X-Robots-Tag: noindex, nofollow`, `/robots.txt` — `Disallow: /`, в разметке `<meta name="robots">`. `/products` по-прежнему уводит на вход;
- **описания в бою**: `import:site-products` обновил 214 товаров, 1 без изменений, создано 0. В базе описания у 214 из 221 товара (6 демо и «Подиум треугольный» — без);
- **галереи в бою**: `import:site-images` загрузил 695 картинок, ошибок нет. Итого 910 картинок, 283 МБ; база выросла с 64 МБ до 322 МБ;
- **диск сервера: 2,5 ГБ свободно из 9,8 ГБ** (было 2,8 до картинок).

**Про бэкапы — важное, найдено при выкате.** Каталог `/backups` в боевом Postgres **пуст**: `scripts/backup-db.sh` ни разу не отработал, ежедневная выгрузка не заведена (в cron её нет). Требование PRD «бэкап каждый день, хранение 30 дней» не выполняется — и это отдельная дыра, независимо от картинок.

Когда бэкапы включат, картинки сделают их тяжёлыми: в plain-дампе `bytea` разворачивается в hex (вдвое), а gzip жмёт уже сжатые JPEG плохо — ориентировочно ~300 МБ на дамп. Тридцать дней хранения — около 9 ГБ, на диск в 9,8 ГБ это не влезет. Прежде чем включать ежедневную выгрузку, решить одно из: хранить меньше дней (`BACKUP_KEEP_DAYS`), вывозить дампы за пределы сервера (этого и так требует PRD), или выносить файлы картинок в объектное хранилище.

## Модели авто: выбор совместимости из справочника (M3, 2026-09-24)

Совместимость в карточке товара выбирается кнопками-переключателями из нового справочника «Модели авто» (`DictionaryItem` типа `CAR_MODEL`, раздел в «Справочниках и настройках»), а не вводится текстом через запятую. Решение — `docs/DECISIONS.md`.

- миграции `20260924210000_car_model_dictionary` (значение enum) и `…_backfill` (22 модели с vanproject.ru из PRD, `ON CONFLICT DO NOTHING`) — двумя файлами, как у источников заказов: новое значение enum нельзя использовать в той же транзакции. Накатаны на общую dev-базу `migrate deploy`, `migrate diff` — пусто;
- домен `src/domain/product/compatibility.ts` (+ 7 тестов): порядок по справочнику, неизвестные модели, переключение;
- сервер: `checkCompatibility` в `src/server/products/service.ts` — принимает только модели справочника (включая выключенные) и уже стоящие у товара названия, сохраняет в порядке справочника; `renameDictionaryItem` для модели переименовывает её и у товаров (`array_replace`, в той же транзакции); `deleteDictionaryItem` не удаляет модель, указанную у товаров; `getCarModels` — включённые модели для формы;
- `src/server/products/compatibility.db.test.ts` (4 теста по живой БД) — локально не запустить (нет `CREATEDB`), пройдут в CI;
- `pnpm check` и `pnpm build` зелёные. **В браузере не проверено**: расширение Chrome после перезагрузки машины не подключилось.
- **Выкачено 24.09.2026** (коммит `2053348`, вместе с выключенным SLA и уведомлениями): CI с тестами по живой БД и деплой зелёные, приложение `healthy`, `/login` — 200, все миграции от 24.09 накатились, в справочнике 22 модели. Первый CI упал на `export.db.test.ts` — тест ждал колонку «Просрочен», убранную вместе с SLA; исправлено.

У товаров из каталога сайта совместимость пустая (импорт её не переносит) — заполняется руками в карточках. Поиск по совместимости и раньше шёл по части названия (`name-match.ts`), пункт «требует названия целиком» в известных проблемах устарел и убран.

## Описание товара (M3, 2026-09-24)

Описания товаров перенесены с сайта; решение — `docs/DECISIONS.md`, «Описание товара».

- разбор `parseProductDescription` в `site-catalog.ts`: вкладка «Описание», блок берётся по балансу `<div>`, HTML сводится к тексту (абзацы переводом строки, пункты «• »), скрипты и стили выбрасываются. Сущности HTML раскодируются шире прежнего — типографика Word и числовые (+ 4 теста);
- миграция `20260924010000_product_description` (`Product.description`, TEXT) — написана руками и сверена с `prisma migrate diff`, применена к общей dev-базе и к `buscom_test`. В бою её ещё нет;
- импорт: `description` в `siteProductRowSchema` **необязательный** — прогон старого `site-products.json` (снятого до 24.09) описания не сотрёт; явный `null` чистит. Повторный прогон обновляет описание по сайту, как название и цену (+ 1 тест по БД);
- карточка товара: поле «Описание» под совместимостью, с подписью, что следующий прогон каталога перезапишет правки. В CSV-выгрузку добавлена колонка «Описание» — многострочная ячейка экранируется кавычками (+ 1 тест);
- **в базе разработки 24.09**: каталог перевыгружен и залит — описания у **214 товаров из 215** (без описания только «Подиум треугольный», его нет и на сайте), маркеры списков сохранились у 24. Повторный прогон — «без изменений: 215». Проверено в браузере: описание в карточке, правка руками сохраняется, следующий импорт возвращает текст сайта (обновлено: 1);
- **в бою с 24.09** — см. «Выкат 24.09.2026» выше.

## Админка закрыта от поисковиков (2026-09-24)

Владелец спросил про индексацию. На живом `erp.bus-com.ru` заголовка `X-Robots-Tag` **не было**: правило из `Caddyfile` не работает, потому что HTTPS терминирует прокси хостинга и Caddy в контуре не участвует. В поиске страниц ERP не нашлось — успели до того, как их проиндексировали.

Запрет перенесён в приложение, чтобы не зависеть от того, что стоит перед ним: заголовок `X-Robots-Tag` на все ответы (`next.config.ts`), `<meta name="robots">` в корневом layout, `/robots.txt` с `Disallow: /` (`src/app/robots.ts`, маршрут выведен из-под проверки сессии в `src/proxy.ts`). Проверено локально: все три отдаются, `/products` по-прежнему уводит на вход. **В бою заработает только после выката** — проверить `curl -I https://erp.bus-com.ru/login | grep -i x-robots`.

## Картинки товара (M3, 2026-09-22)

Аватарка товара с заделом под галерею; решение — `docs/DECISIONS.md`, «Картинки товара».

- миграция `20260922100000_product_images` — таблица `ProductImage` (файлы в `bytea`, полный размер и превью, `sourceUrl` с сайта). Накатана на `buscom_erp`;
- домен `src/domain/product/images.ts` (тип по сигнатуре, лимит 5 МБ, + 3 теста), разбор картинок сайта в `site-catalog.ts` (+ 2 теста);
- сервер: `products/images.ts` (замена аватарки новой записью, удаление, чтение), `products/site-images.ts` (импорт, + 2 теста по БД), маршрут `/api/product-images/[id]`, действия загрузки и удаления;
- экраны: превью в таблице товаров, блок картинки в карточке товара (загрузить, заменить, удалить, открыть в полном размере);
- `pnpm import:site-images` в базе разработки: 215 аватарок (39 МБ + 3,6 МБ превью), повторный прогон ничего не качает. Проверено в браузере: превью в списке, карточка, отдача полной и превью с кешем, 404 на чужой id, загрузка своей картинки (новый id), возврат картинки сайта импортом;
- **в бою с 22.09** — см. «Справочник категорий».

### Все картинки, а не только заглавные (2026-09-23)

Владелец попросил перенести в ERP все картинки товаров. Решение — `docs/DECISIONS.md`, «Галерея товара».

- `parseProductImages` берёт превью и у дополнительных картинок (74×74; у главной по-прежнему 228×228) — каталог перевыгружен: **215 товаров, 910 картинок**, у всех есть превью;
- `importSiteImages` синхронизирует галерею целиком: порядок как на сайте, уже загруженное не качается, снятое с сайта убирается, загруженное руками (`sourceUrl` пуст) сдвигается за картинки сайта и не затирается. Скачивание — до транзакции, транзакция с запасом по времени (60 с): по туннелю запись небыстрая;
- сервер `products/images.ts` переписан на галерею: `addImages` (пачкой, в конец), `deleteImage` и `makeImageMain` по id картинки с перенумерацией, `renumberImages`. Действия — `uploadProductImagesAction`, `deleteProductImageAction`, `makeProductImageMainAction`. Тесты: `images.db.test.ts` (6) и переписанный `site-images.db.test.ts` (5);
- карточка товара: ряд миниатюр с прокруткой (у сиденья S21 их 45 — без ограничения высоты поля уезжали вниз), значок «Главная» на первой, по наведению — «Сделать главной» и «Удалить», щелчок открывает полный размер. Выбрать можно несколько файлов сразу, уходят по одному (лимит тела Server Action 6 МБ);
- **в базе разработки 23.09**: загружено 695 картинок, ошибок нет, итог — 910 картинок у 215 товаров, **283 МБ** полных и 7,4 МБ превью (было 39 МБ). Повторный прогон ничего не качает. Проверено в браузере: превью в списке, галерея в карточке (45 картинок, главная 228×228, остальные 74×74);
- **в бою с 24.09** — см. «Выкат 24.09.2026» выше;
- грабли: первая версия «Сделать главной» и «Удалить» перенумеровывала всю галерею по одной картинке и у сиденья с 45 картинками не укладывалась в пятисекундный срок транзакции Prisma — действие молча не срабатывало. Теперь обе двигают порядок сдвигом (`updateMany` с `increment`/`decrement`), два запроса вместо полусотни; перенумерация целиком осталась только в импорте и пропускает картинки, уже стоящие на месте;
- попутно починено: колонка с превью в списке товаров схлопывалась до нуля, когда таблица шире окна (`min-w-14` в `products-table.tsx`) — на узком окне аватарок не было видно вообще.

## Статусы упрощены до четырёх (2026-09-21)

По решению владельца глобальных статусов четыре: «Создан», «В работе», «Выполнен», «Отменён»; промежуточные этапы — в цепочках поставщиков. Подробности и что куда перенесено — `docs/DECISIONS.md`, «Статусная модель упрощена».

- миграция `20260921220000_order_statuses_simplified` (руками: пересоздание enum, перевод заказов, прежние переходы журнала — текстом в комментарий) накатана на `buscom_erp`: 3015 «Выполнен», 5 «В работе», две записи журнала с прежними статусами сохранены текстом;
- правила «отмена оплаченного — только руководитель» и «состав после оплаты — только руководитель» теперь по сумме платежей; автопереход в «Оплачен» убран; трек-номер ТК — при переводе в «Выполнен»;
- `pnpm check` зелёный (267 тестов), PRD обновлён (статусная модель, бизнес-правила, критерии приёмки);
- **в браузере не проверено**: Prisma-клиент перегенерирован — нужен перезапуск `pnpm dev`.

## Опции товара (M3, 2026-09-21)

У товара группы опций с вариантами и надбавкой к цене; модель и правила — `docs/DECISIONS.md`, «Опции товара».

- миграция `20260921200000_product_options` — таблицы `ProductOption`, `ProductOptionValue`, поле `OrderItem.options` (снимок). Накатана на `buscom_erp`;
- домен `src/domain/product/options.ts` (+ 7 тестов): снимок выбора, цена с надбавками, подпись; разбор опций сайта в `site-catalog.ts` (+ 3 теста);
- сервер: `replaceProductOptions` в `products/service.ts`, `orders/options.ts` (снимок при создании и правке состава), опции в поиске товаров и в счёте PDF;
- экраны: блок «Опции» в диалоге товара (группы, варианты, надбавки, «обязательная»), опции под названием в таблице товаров; при добавлении позиции товара с опциями — шаг выбора с ценой (карточка заказа и форма нового заказа), выбранные опции под названием позиции;
- **каталог сайта перевыгружен с опциями**: у 60 товаров 85 групп и 496 вариантов, повторный прогон — 215 «без изменений». Из 32 товаров с ценой 0 без опций остались 3 (два клея и «Подиум треугольный») — у них на сайте действительно «цена по запросу»;
- `pnpm check` зелёный (274 теста); тесты по БД `orders/options.db.test.ts` (3) и новый в `site-import.db.test.ts` — прогонит CI;
- **в браузере не проверено**: запущенный `pnpm dev` держит старый Prisma-клиент (`/products` и поиск товаров падают на `options`). После перезапуска прокликать: опции в диалоге товара, добавление «Стекла на Ford Transit LWB» и «Сиденья Интурист - Люкс» в заказ, счёт PDF.

## Каталог перенесён с bus-com.ru (M3, 2026-09-21)

Сайт — OpenCart. Перенос в два шага, как импорт клиентов и заказов:

- `pnpm fetch:site-products` — обходит сайт (sitemap → страницы товаров, меню → листинги категорий, пауза 300 мс между запросами) и пишет `misc/site-products.json`. В базу не ходит;
- `pnpm import:site-products misc/site-products.json [--dry-run]` — раскладывает файл в каталог. Ключ — `product_id` сайта в `Product.externalId`, повторный прогон обновляет товары по сайту (название, артикул, категория, цена, включён/выключен) и не трогает поставщиков, закупочные цены и совместимость.

Разбор страниц — `src/domain/product/site-catalog.ts` (+ 11 тестов), импорт — `src/server/products/site-import.ts` (+ 3 теста по БД, прогонит CI). Откуда что берётся на сайте — в шапке `site-catalog.ts`.

**Прогон 21.09.2026:** в sitemap 654 адреса товаров, без дублей — 215 товаров (один товар лежит под `/paz/…`, `/russia/…`, `index.php?product_id=…`). Все 215 заведены в `buscom_erp`, повторный прогон — 215 «без изменений». В каталоге теперь 221 товар: 215 с сайта и 6 демо.

**Что заметить в данных:**

- **33 артикула с суффиксом.** На сайте артикул повторяется (12 шторок под `SHT02`, три `P06`, три `PER03` и т.д.), а в ERP он уникален. Первый по `product_id` остался как есть, остальные — `SHT02-441`. Скорее всего, на сайте это ошибки заведения — стоит показать владельцу список (его печатает импорт);
- **32 товара с ценой 0** — на сайте «цена по запросу». В заказ они подставятся с нулём, цену менеджер ставит руками;
- **18 товаров без категории** — услуги и позиции раздела «Переоборудование» («Установка шторок», «Подиум…»), которых нет в каталожном меню;
- **совместимость не заполнена**: на сайте её нет отдельным полем — модели авто только в названиях. «Производитель» на сайте — в основном страна («Россия», «Турция»), в схеме товара такого поля нет, не переносится.

## Источники заказов в справочнике (M8, 2026-09-21)

Источники из фиксированного списка в коде стали справочником в «Справочниках и настройках»: добавить, переименовать, выключить. «Сайт» и «Прежняя ERP» — системные, только переименовываются. В форме нового заказа и в карточке (строка «Источник» в шапке) выбор из справочника; смена пишется в журнал; у заказов с сайта и архивных источник не меняется. Фильтр списка, колонка и выгрузка CSV берут название из справочника. Заодно в редакторе справочников появилось переименование — для всех трёх списков. Следом добавлено удаление (с подтверждением в строке): причины отмены и ТК удаляются свободно — в заказах они хранятся текстом; источник удаляется, только если его нет ни в одном заказе, иначе его выключают; системные не удаляются. Проверено в браузере: временный пункт ТК удалён, «Телефон» (стоит в заказе №3018) удалить не дало с понятным сообщением. Модель — в `docs/DECISIONS.md`.

- миграции `20260921180000_order_sources` и `20260921180100_order_sources_backfill` накатаны на `buscom_erp`: шесть пунктов заведены, все 3018 заказов привязаны (3017 — «Прежняя ERP», 1 — «Телефон»), непривязанных 0;
- `pnpm check` зелёный (253 теста); тесты по БД `src/server/orders/source.db.test.ts` (5 штук) локально не прогнаны — нет тестовой базы, прогонит CI;
- **в браузере не проверено**: запущенный `pnpm dev` держит старый Prisma-клиент (`/admin/dictionaries` падает на `ORDER_SOURCE`). После перезапуска — прокликать справочник, новый заказ, смену источника в карточке, фильтр списка.

## Поставщики (M9, 2026-09-21)

Запрос владельца: раздел «Поставщики» с полями как у клиентов, цепочка статусов в карточке, поставщики с ценой у товара, флоу заказа по поставщикам. Модель решения — в `docs/DECISIONS.md` («Поставщики: цепочка этапов — подстатусы…»), требования — M9 в PRD.

Сделано:

- миграция `20260921150000_suppliers` — таблицы `Supplier`, `SupplierStage`, `ProductSupplier`, `OrderSupplierTrack`, у `OrderItem` поля `supplierId` и `purchasePriceKopecks`, событие журнала `SUPPLIER_STAGE_CHANGED`. Только добавления, существующие данные не трогает. SQL сгенерирован `prisma migrate diff` (на этой машине у роли нет `CREATEDB`, теневая база не создаётся), применён на `buscom_erp`, после этого `migrate diff --exit-code` — схема и база совпадают;
- домен `src/domain/supplier/stages.ts` (+ 13 тестов) и проверка треков в `assertTransition` (+ 4 теста): в «Отправку» — только когда все поставщики на последнем этапе;
- сервер: `server/suppliers/{service,list}.ts`, привязки в `products/service.ts`, `orders/suppliers.ts` (снимок закупки, синхронизация треков, смена этапа с журналом); `createOrder`, `updateOrderItems` и `changeOrderStatus` их используют;
- экраны: `/suppliers` (список, поиск, «Новый поставщик» с цепочкой), `/suppliers/[id]` (данные, редактор цепочки, товары поставщика, удаление); в диалоге товара — блок «Поставщики» с закупочной ценой, в таблице товаров — колонка; в позициях заказа и в форме нового заказа — выбор поставщика; в карточке заказа — блок «Поставщики» с треками и кнопками шага;
- `pnpm check` зелёный (250 тестов). Тесты по живой БД — `src/server/suppliers/suppliers.db.test.ts`, 9 штук — **локально не прогнаны**: базы `buscom_test` на этой машине нет, а роль `buscom` не может её создать. Их прогонит CI.

**Не проверено в браузере.** Запущенный `pnpm dev` держал Prisma-клиент, сгенерированный до миграции: `/suppliers` падал на `db.supplier` = `undefined`. Нужен перезапуск dev-сервера — после него прокликать: завести поставщика с цепочкой → привязать к товару → заказ с этим товаром → шаги по этапам → «Отправка» до и после последнего этапа.

## Выгрузка заказов из прежней ERP (2026-09-21)

Файл `misc/orders.csv` — 3017 заказов, 32 колонки, cp1251, разделитель `;`, период с 09.02.2017 по 18.08.2026. Как и клиентская база, лежит в `misc` и в репозиторий не попадает.

Чего в источнике нет и что из этого следует:

- **позиций заказа нет** — только строка «Описание» и общая сумма. Заказ получает одну позицию с артикулом `ИМПОРТ` на всю сумму, иначе итог разошёлся бы с суммой к оплате;
- **номер заказа не уникален**: 1892 разных номера на 3017 строк. Ключом повторного прогона служит `ID` строки — он уникален и ложится в `(source, externalId)`, уже имеющийся в схеме;
- **статус отправки заполнен у 1544 из 3017** и в наши статусы не переводится однозначно. По решению владельца все перенесённые заказы — «Выполнен» (архив);
- **клиента в строке нет, есть имя и email**. Нашлись 2982 из 3017 по имени, остальным 35 импорт завёл клиентов сам;
- **аналитика прежней ERP** (расход, прибыль, маржинальность, комиссия, «Наша компания») выброшена — места в заказе ей нет.

Сделано:

- миграция `20260921120000_order_source_legacy` — источник `LEGACY` («Прежняя ERP»), чтобы архив отделялся в списке от заказов «прочее»;
- `src/domain/order/legacy-import.ts` — разбор строки (даты московские, суммы в копейки, «Самовывоз» → самовывоз, переплата не переносится) + 10 тестов;
- `src/server/orders/import.ts` — перенос с перенумерацией + 9 тестов по живой БД;
- `pnpm import:orders misc/orders.csv [--dry-run] [--renumber]`;
- список источников свёлся к `ORDER_SOURCES` / `ORDER_CREATE_SOURCES` — он был продублирован в четырёх местах, и фильтр по `LEGACY` из-за этого сначала молча не работал.

**Номера.** Архив занял 1..3017 по дате (старший заказ — первый), а 14 рабочих заказов сдвинулись на 3018..3031; последовательность передвинута следом. Сдвиг требует явного `--renumber`: номер заказа печатается в счёте.

**Прогон.** Тестовая база: создано 3017, клиент найден у 2983, заведено 34. Повторный прогон — создано 0. **Суммы сошлись с файлом копейка в копейку**: 84 326 224,71 ₽ всего, 78 743 051,71 ₽ оплачено. База разработки: те же 3017 заказов, 3031 всего, 4345 клиентов. Карточка архивного заказа проверена в браузере — позиция, платёж, доставка и запись в журнале со сведениями прежней ERP на месте.

**Вторая машина разработки** (21.09.2026): база была пустой — ни клиентов, ни заказов. Накатаны `customer_import_fields` и `order_source_legacy` (`migrate deploy`), затем оба импорта из `misc/`. Итог: 4340 клиентов (4306 из выгрузки + 34 заведены импортом заказов), 3017 заказов с номерами 1..3017, суммы те же — 84 326 224,71 ₽ всего, 78 743 051,71 ₽ оплачено. Демо-заказов и демо-клиентов здесь нет. Замечание: в `--dry-run` импорт заказов показывает «клиент заведён» больше настоящего (94 против 34) — без записи один и тот же новый клиент не находится для следующих своих заказов.

**Четыре действующих заказа «В работе» (21.09.2026, по просьбе владельца, база разработки).** Данные сняты со страниц прежней ERP (clientbase, `view_line2.php?table=43&line=ID`) через браузер с сессией владельца — включая позиции счёта, которых нет в CSV-выгрузке:

- ID 3240 (№3016) и 3241 (№3017) уже были перенесены массовым импортом как «Выполнен» со сводной позицией `ИМПОРТ`. В прежней ERP они «В производстве» и не оплачены. Позиции заменены настоящими из счёта (STF3 × 30 и S07 × 1, привязаны к каталогу сайта), статус исправлен на «В работе» прямой записью — **в обход `assertTransition`**, потому что переход «Выполнен → В работе» не предусмотрен, а это исправление данных импорта, не рабочий переход. Оба изменения записаны в журнал заказа (автор — система);
- ID 3242 (№3019, Каплюхин М. С.) и 3243 (№3020, Кондауров Э. В.) — новые, в CSV их не было: заведены с источником «Прежняя ERP», статусом «В работе», позицией SK24 (35 и 30 шт по 100 ₽), оплатой наличными за 21.09 и новыми клиентами. Ответственный не назначен — пользователя «Бражник» в ERP нет.

Суммы сошлись со счетами прежней ERP. **Открытый вопрос к владельцу:** правило «все архивные — Выполнен» задело и другие незакрытые заказы. По CSV в 2026 году, кроме этих двух, ещё 4 «Ждем оплаты» и 1 «Проблемный» (подсчёт приблизительный — в части строк кавычки сдвигают колонки). Для боевого импорта стоит решать это правилом (например, «Ждем оплаты» → «Ждёт оплаты»), а не руками.

**Осталось:** боевой прогон (после выката), и там понадобится `--renumber`, если в бою уже заведут заказы.

## Склад убран из проекта (2026-09-20)

Требование владельца: **склада у компании нет**, количества товара не ведутся. Это отменяет часть PRD (M3 «остаток и резерв», роль «Логист/склад», бизнес-правило «Резерв остатка») и отвечает на открытый вопрос PRD «где сейчас ведутся остатки» — нигде. Решено убрать весь складской блок; статус «Сборка» заменён на «Отправка».

**Сделано и проверено (`pnpm check` зелёный, 202 теста):**

- миграция `20260920170000_warehouse_removed` — написана руками, а не сгенерирована: `ALTER TYPE ... RENAME VALUE` вместо DROP/ADD (иначе заказы в статусе пропали бы), пересоздание `UserRole` с переводом складских сотрудников в менеджеры, `DROP COLUMN` у `Product` для `stock`, `reserved`, `madeToOrder`, `leadTimeDays`. Применена на локальной БД;
- схема: `Product` без количеств, `UserRole` без `WAREHOUSE`, `OrderStatus.ASSEMBLY` → `SHIPPING` («Отправка»);
- домен: удалены `order/reservation.ts` и его тесты; в `status.ts` переходы `PAID → SHIPPING → SHIPPED` отданы менеджеру; `sla.ts` и `settings.ts` знают про `SHIPPING`; из `role.ts` убрана роль «Склад»;
- сервер: удалён `orders/reservation.ts`, из `status.ts` и `items.ts` убран пересчёт резерва, из `create/delivery/payments/assignment` — проверки складской роли, из `products/service.ts` — `setProductStock` и `canEditStock`, вид «К отгрузке» убран из `orders/list.ts`;
- экраны: таблица и форма товара без остатка, резерва, «свободно», «под заказ» и срока; фильтр «Нет в наличии» убран; подбор позиции в заказе больше не пишет «в наличии»; из карточки заказа ушли складские ограничения; в админке роль «Склад» не предлагается;
- выгрузка товаров в CSV — 6 колонок вместо 11; тесты обновлены;
- сид: демо-каталог без количеств.

**Доделано во второй заход (`pnpm check` зелёный, 202 теста):**

- **комплектовочный лист убран**: `buildPackingList` из `src/server/documents/invoice.ts`, значение `packing-list` из маршрута `documents/[kind]`, кнопка с карточки заказа. Из печатных форм остался счёт; маршрут `[kind]` оставлен с одним значением — по PRD ещё открыт вопрос про УПД и товарный чек;
- **PRD обновлён**: роль «Логист/склад» убрана из таблицы ролей (стало три роли), M3 без остатка и резерва, M1.8 без комплектовочного листа, правила «Резерв остатка» и «Нехватка остатка» убраны, статусная модель и mermaid-схема переведены на `SHIPPING` («Отправка»), вкладка «К отгрузке» и критерии приёмки про склад убраны, вопрос «где ведутся остатки» закрыт. **Живой документ (ссылка в шапке PRD) правит человек — там правки пока нет**;
- **`docs/DECISIONS.md`**: две записи — «Склада в проекте нет: блок остатков удалён целиком» (отменяет прежнюю запись про пересчёт резерва) и «Миграция удаления склада написана руками»;
- **комментарии** про склад поправлены в `server/customers/service.ts`, `server/orders/delivery.ts`, `server/orders/status.ts`. Намеренно оставлены пояснения в `server/orders/list.ts`, `server/products/export.ts` и `prisma/schema.prisma` — они объясняют, почему чего-то нет;
- **миграция накатана на базу второй машины разработки** (`prisma migrate deploy`), Prisma-клиент перегенерирован. Грабли: без `pnpm db:generate` типы врут и `pnpm check` падает на `SHIPPING` и `WAREHOUSE` — клиент не коммитится, после `git pull` на другой машине генерацию надо повторить.

**Прогон экранов после удаления (2026-09-20, dev на второй машине):**

- список заказов: вкладки «Все 14 / Мои 11 / Без менеджера 3 / Просроченные 3» — вида «К отгрузке» нет; среди чипов статусов «Отправка» вместо «Сборки»;
- карточка заказа №14 («Оплачен»): единственный переход вперёд — «Отправка»; №15 («Отправка»): переход «Отгружен». Оба доступны администратору, складских ограничений в карточке не осталось;
- печатные формы: в шапке карточки только «Счёт PDF». `GET /api/orders/15/documents/invoice` → 200 и PDF «Счёт № 15» открывается; `…/documents/packing-list` → 404 «Неизвестный документ»;
- товары: колонки «Артикул, Название, Категория, Цена, Совместимость» — без остатка, резерва, «свободно» и «под заказ»; из фильтров остались категория и «Скрытые», «Нет в наличии» убран;
- подбор позиции в заказе: в выдаче название, артикул и цена, приписки «в наличии» нет;
- администрирование: в списке ролей ровно три — «Менеджер», «Руководитель», «Администратор».

Сбоев и следов склада не найдено.

**Грабли со входом на этой машине:** пароль администратора в базе не совпадал с `SEED_ADMIN_PASSWORD` из `.env`, вход давал «Неверный email или пароль» (форма в dev подставляет данные из `.env`). Сид пароль не перетирает — он пропускает уже существующего админа. Вылечено удалением записи `account` с `providerId: "credential"` и созданием новой с хешем от `hashPassword` из `better-auth/crypto` (как в `prisma/seed.ts`). Заодно вычищена запись в `FailedLogin`.

**Осталось сделать:**

- [x] **Прогнаны экраны в браузере** — см. «Прогон экранов после удаления» выше;
- [x] **Выкатить на боевой сервер** — выкачено, бой с тех пор обновлялся не раз (последний выкат — 24.09.2026, `39fc64c`);
- [ ] **Поправить живой документ PRD** (ссылка в шапке `docs/PRD.md`) — снимок в репозитории уже обновлён.

## Этап 2 и дальше

См. PRD, «Роадмап». Интеграция с сайтом блокируется открытыми вопросами к заказчику (платформа сайта, способ приёма заказов), а деплой — нет, и он начат.

### Разворачивание (2026-09-20) — контур поднят

Подробности и рунбук — `docs/DEPLOY.md`. Коротко: сервер Джино.VPS, Ubuntu 22.04, OpenVZ, 1 ядро / 1.5 ГБ / 9.8 ГБ диска, выделенного IP нет.

- [x] Автодеплой из GitHub Actions работает целиком: сборка образов → GHCR → выкат → проверка живости
- [x] Postgres, миграции и приложение подняты, контейнеры `healthy`; снаружи `https://ba5699d52128.vps.myjino.ru/login` отдаёт 200
- [x] Первый администратор заведён сидом, вход проверен живым запросом: 200 и роль ADMIN
- [x] Секреты и переменные репозитория заведены (`DEPLOY_SSH_KEY`, `DEPLOY_HOST`, `DEPLOY_USER`, `DEPLOY_KNOWN_HOSTS`, `DEPLOY_PORT`, `DEPLOY_PATH`)
- Логин и пароль администратора смотреть командой из `README.md` («Боевой контур») или `docs/DEPLOY.md` («Логин и пароль администратора») — они в `.env.production` на сервере
- [x] **`erp.bus-com.ru` привязан к серверу и SSL подключён** (20.09.2026, владельцем аккаунта). Проверено с машины разработки: `https://erp.bus-com.ru/login` — 200 с валидным сертификатом, `/orders` — 307 на вход.
- [x] **Перенаправление http → https включено** (24.09.2026, владельцем в панели Джино). Проверено `curl -I`: `http://erp.bus-com.ru/login` → 302 на `https://erp.bus-com.ru/login`, строка запроса сохраняется. Как было до этого: `http://erp.bus-com.ru/login` отдавал 200. По http cookie сессии (`Secure`) не сохраняется, то есть войти нельзя — а браузер идёт на http, если адрес набран без схемы Диагностика была такая: DNS в порядке (CNAME → `ba5699d52128.vps.myjino.ru` → `81.177.135.137`), но отвечал nginx провайдера с 404 — у сервера нет выделенного IP, порты 80/443 проксирует Джино и делает это **только для доменов, привязанных к серверу**. То есть дело было не в DNS и не в приложении, а в привязке; менять записи не понадобилось. Проверено владельцем в браузере — с машины разработки домен не проверить, его режет VPN (hidemy.name), нужно исключение для `bus-com.ru`
- [x] Копия бэкапов вне сервера — на машине владельца (24.09.2026). Мониторинг и алерт по заказам — не нужны (решение владельца 24.09.2026)
- [x] SMTP; убрать демо-каталог из 6 товаров, который завёл сид (24.09.2026, владельцем)

### Что было сделано под разворачивание

Готово к первому запуску на сервере, рунбук — `docs/DEPLOY.md`.

- [x] `output: "standalone"` в `next.config.ts` — сборка в самодостаточный сервер
- [x] `Dockerfile`: многоступенчатый, цели `runner` (приложение, без pnpm и исходников) и `migrator` (Prisma CLI для миграций и сида). Node 22 — как в CI. Движка Prisma в образе нет: driver adapter даёт чистый TS-клиент
- [x] `docker-compose.prod.yml`: Postgres (порт наружу не публикуется), одноразовый `migrate`, `app`, Caddy с Let's Encrypt. `app` стартует только после успешных миграций
- [x] `Caddyfile`, `.dockerignore`, `scripts/backup-db.sh` (ежедневный `pg_dump`, хранение 30 дней, падает при подозрительно малом файле)
- [x] Production-сборка проверена локально: вход, список, карточка, матрица прав по четырём ролям, оба PDF
- [x] **Образ собирается** — в GitHub Actions при каждом выкате (сборка → GHCR), Docker на машине разработки для этого не нужен
- [x] От человека получены сервер (Джино.VPS, Ubuntu 22.04), домен `erp.bus-com.ru` и `.env.production` с секретами
- [x] Копия бэкапов вне сервера — `scripts/pull-backups.ps1` (24.09.2026)
- [x] ~~Мониторинг (Sentry/GlitchTip) и алерт «за 3 рабочих часа не пришло ни одного заказа»~~ — не нужны (24.09.2026)
- [x] Автодеплой по пушу в `main` работает — см. «Разворачивание» выше; руками по рунбуку — запасной путь

## Известные проблемы и долги

- ~~**Сессии деактивированного пользователя не отзываются.**~~ Отключение на `/admin/users` удаляет их в той же транзакции (`setUserActive` в `src/server/users/service.ts`), смена пароля — тоже. Осталась оговорка: `POST /api/auth/sign-in/email` напрямую по-прежнему выдаст cookie и деактивированному (Better Auth про `isActive` не знает), но экраны его разворачивают на `/login` — `getSessionUser` такую сессию не признаёт.
- ~~**Экрана 403 нет.**~~ Сделан 2026-09-20: `requirePageUser` + `src/app/(app)/forbidden.tsx`, флаг `experimental.authInterrupts` включён. Правило: страница — `requirePageUser` (403-экран), действие или сервис — `requireUser` (`ForbiddenError` → тост). Route handlers по-прежнему отдают 403 сами.
- ~~**Ограничение входа считает все попытки, а не только неудачные**~~ — сделано 2026-09-20: счётчик неудач в таблице `FailedLogin` по email (`src/domain/auth/login-throttle.ts` + `src/server/auth/login-attempts.ts`, миграция `20260920131928_failed_login_attempts`). 10 неудач за 15 минут закрывают вход на 15 минут; пока он закрыт, попытки не пишутся и верный пароль тоже не пускает. Успешный вход счётчик обнуляет. Лимитер Better Auth остался защитой от флуда с адреса, порог поднят до 60 за 15 минут, чтобы не срабатывать раньше правила PRD. Проверено 11 доменными тестами, 8 по живой БД и руками в браузере.
- **Разделы «Заказы», «Клиенты», «Товары» сделаны целиком** — заглушек в меню не осталось.
- **Выбранная руками главная картинка сбрасывается следующим импортом.** Для товаров с сайта порядок галереи задаёт сайт, и `import:site-images` каждый раз выстраивает картинки как на странице товара. Если это помешает — нужен признак «закреплено человеком» у картинки (миграция), решать с владельцем.
- **Картинки заняли 283 МБ в базе** (910 штук против 39 МБ у 215 аватарок). Бэкап и перенос в бой идут вместе с данными, так что дамп теперь тяжелее на эту величину. Если каталог вырастет в разы — переносить файлы в объектное хранилище (`docs/DECISIONS.md`, «Картинки товара»).
- **Праздники РФ в SLA не учитываются** — считаются обычными рабочими днями (PRD: производственный календарь после MVP). Москва зашита как UTC+3 без перехода на летнее время; если его вернут, понадобится библиотека часовых поясов.
- **Вкладка «Просроченные» потребует расчёта SLA в SQL.** `slaState` считает в приложении, а список постраничный — на этапе 1 нужно либо хранить дедлайн в заказе, либо считать фильтр запросом.
- **Тесты серверного слоя стали возможны:** `vitest.config.mts` подменяет пакет `server-only` заглушкой (`src/test/server-only-stub.ts`), иначе он намеренно падает вне серверной среды Next.
- ~~**Интеграционные тесты локально не прогонялись.**~~ База `buscom_test` заведена 2026-09-20, роли `buscom` выдан `CREATEDB`. Прогон: `RUN_DB_TESTS=1 DATABASE_URL=postgresql://buscom:buscom@localhost:5432/buscom_test pnpm test`. Тесты в `*.db.test.ts` по-прежнему идут только при `RUN_DB_TESTS=1` и только по базе, в имени которой есть `test` — `resetDb` чистит все таблицы, и ошибка стоила бы данных.
- **Тесты по живой БД идут по очереди, а не параллельно.** На первом же прогоне два файла `*.db.test.ts` столкнулись на одной базе: `TRUNCATE` из одного вычищал данные другого — взаимоблокировка Postgres (40P01) и чужой `productId` в выборке. `vitest.config.mts` при `RUN_DB_TESTS=1` ставит `fileParallelism: false`. Цена — прогон 14–21 с вместо 1,3 с; обычный прогон без БД остался параллельным. Если файлов по БД станет заметно больше, дешевле дать каждому воркеру свою схему, чем держать всё последовательным.

- **В dev-БД израсходованы номера заказов 1 и 2** — проверочный прогон сервиса. В базе сейчас 12 демо-заказов (№3–14) для проверки списка; перед боевым запуском их надо убрать.
- ~~**Карточку не кликали руками.**~~ Прокликана 2026-09-20 в браузере, см. «Проход по экранам».
- **Причины отмены теперь в справочнике**; константа в `src/domain/order/cancel-reasons.ts` осталась как набор по умолчанию для пустой базы и как источник для сида.
- ~~**Экраны администрирования не кликали руками**~~ — прокликаны 2026-09-20 вместе с остальными.
- **Временный пароль показывается тостом** на экране пользователей, письмо не отправляется — сотрудник может сам запросить ссылку на `/forgot-password`.
- **SMTP не настроен.** Пока `SMTP_HOST` пуст, письма не уходят, а ссылка пишется в лог сервера с предупреждением. Перед запуском заполнить SMTP-переменные (см. `.env.example`).
- **Замер на 100 000 заказов сделан** (2026-09-20, production-сборка, локальный Postgres 16, данные потом удалены). p95 по HTTP: список 56 мс, вкладки 39–40 мс, фильтры 27 мс, страница 500 — 34 мс, клиенты 48 мс, товары 14 мс. Самое медленное — поиск одним полем: 210 мс по номеру, 284 мс по телефону. Требование ≤ 500 мс выполняется с запасом, но поиск — то место, которое первым упрётся в предел: он идёт `OR` по связанному клиенту, одним индексом это не закрыть. Если вырастет — вынести поисковую строку в отдельное поле заказа с GIN-индексом.
- **Поиск по email работает только при вводе адреса целиком** (с `@`); по части домена не ищет. По ИНН — точное совпадение. Если понадобится иначе, править `searchWhere` в `src/server/orders/list.ts`.
- ~~**`prisma migrate dev` на этой машине не работает**~~ — после `ALTER ROLE buscom CREATEDB` (2026-09-20) теневая БД создаётся, `prisma migrate dev` отвечает «Already in sync». Обходной путь через `migrate diff` + `migrate deploy` больше не нужен — новые миграции делать штатным `pnpm db:migrate`.
- ~~**Таблицы Better Auth написаны вручную**~~ — проверено на живой БД 2026-09-19: вход, сессия и дополнительные поля `role`/`isActive` работают. Одна ловушка уже стоила отладки: при входе Better Auth ищет аккаунт с `accountId === user.id`, поэтому в сиде туда пишется id пользователя, а не email.
- **`.claude/settings.json` запрещает `Read(.env.*)`** — это блокирует и `.env.example`: агент может дописывать в него, но не читает и не проверяет на дубли. Заменить на `Read(.env.local)` и `Read(.env.production)` (агенту правка собственных настроек запрещена — делает человек).
- **`gh` установлен и авторизован** — 2.101.0 через winget, лежит в `C:\Program Files\GitHub CLI\gh.exe`, аккаунт `etisnow`, скоупы `repo`, `workflow`, `gist`, `read:org`. В сессии, которая стартовала до установки, его нет в PATH — звать по полному пути; новые сессии подхватят.
- **Два дефекта нашлись только на боевом запуске** (оба починены, см. DECISIONS): пустая переменная окружения из compose валила старт приложения (`.optional()` у Zod не допускает ""), и проверка живости в деплое читала собственный скрипт через `docker compose exec -T`, из-за чего выкат рапортовал успех при нерабочем приложении. Правило на будущее: в скрипте, который приходит по stdin, всем командам, читающим stdin, нужен `< /dev/null`.
- **Генерация PDF ломалась только в production-сборке.** Путь к шрифтам собирался от cwd и не совпадал с раскладкой pnpm в standalone-выводе. Починено через `serverExternalPackages` (см. DECISIONS). Вывод на будущее: всё, что читает файлы своего пакета, проверять на production-сборке — в `pnpm dev` раскладка node_modules другая.
- **Из-под VPN не открываются российские адреса** — ни bus-com.ru, ни боевой сервер. Проверено 2026-09-20: `Test-NetConnection` до `81.177.135.137` (VPS) идёт через интерфейс `hidemy.name VPN OpenVPN Adapter` и не проходит ни на 49265, ни на 80, при этом `github.com:443` и `github.com:22` доступны — значит, режет именно VPN, а не среда сессии. Чтобы агент мог работать с сервером по SSH, VPN нужно отключить или увести адрес сервера мимо туннеля. Данные о сайте по-прежнему брать у заказчика, а не скачивать. **20.09.2026, 15:37 МСК проверено снова: сервер из сессии доступен** — `https://ba5699d52128.vps.myjino.ru/login` отвечает 200, `/orders` даёт 307 на `/login`. То есть ограничение зависит от состояния VPN, а не постоянно.
- **Git-автор первых коммитов** — рабочий адрес из глобального git-конфига; для этого репозитория, возможно, стоит задать `git config user.email` локально.

## Открытые вопросы к заказчику

Список — в PRD, раздел «Открытые вопросы». Ответы записывать туда (и в живой документ), решения — в `docs/DECISIONS.md`.

**24.09.2026 владелец ответил на все вопросы.** SLA не нужен — выключен флагом `SLA_ENABLED = false` в `src/domain/sla.ts`: нет подсветки просрочки в списке, вкладки «Просроченные», колонки «Просрочен» в CSV и нормативов в справочниках; `slaDueAt` по-прежнему считается (`docs/DECISIONS.md`). Что из ответов ещё требует работы:

- **Роли:** в ERP работают двое на равных правах — владелец и партнёр (Дмитрий). Партнёр заведён администратором (24.09.2026, владельцем); роль `MANAGER` и лимит скидки в коде остаются для будущих сотрудников.
- **Справочник ТК:** СДЭК, Деловые линии, ПЭК, КИТ (GTD) — в бою есть (24.09.2026; КИТ заведён владельцем через интерфейс, прежнего пункта «GTD» больше нет).
- **Совместимость товаров:** сделано 24.09.2026 — выбор из справочника «Модели авто», см. раздел «Модели авто: выбор совместимости из справочника».
- **УПД и товарный чек:** понадобятся после открытия ИП (P1, этап 4).
- **Переписка в Max через бота** — тоже после открытия ИП (решение владельца, 25.09.2026), замысел и ограничения — PRD, M6.3. От владельца: ИП, профиль на платформе Max для партнёров с верификацией, бот после модерации и его токен. В коде: обобщить `Email` до сообщения с каналом, опрос бота (long polling, как IMAP), отправка и выбор канала в форме «Переписки».
- **Доступ к админке OpenCart есть** — можно планировать обратную синхронизацию и забор каталога не только парсингом страниц.
