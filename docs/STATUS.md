# Статус проекта

> Читать в начале каждой сессии. Обновлять в конце: отметить сделанное, дописать проблемы, передвинуть «Следующий шаг».
> Требования — `docs/PRD.md`, принятые решения — `docs/DECISIONS.md`.

**Обновлено:** 2026-09-20
**Текущий этап:** 1 — Заказы (M1 + M8) закрыт; сверх него сделаны M2 (клиенты) и M3 (товары)
**Следующий шаг:** пройти все экраны в браузере руками по критериям приёмки из PRD — для агента нужно подключить расширение Claude для Chrome, иначе это делает человек. Дальше — этап 2 по PRD, он упирается в открытые вопросы к заказчику.

## Этап 0 — Фундамент

- [x] Next.js 16 + TypeScript strict + Tailwind 4, pnpm
- [x] Prisma 7 схема по PRD + начальная миграция `20260919000000_init`
- [x] Доменный слой: деньги, статусная машина, итоги заказа, нормализация телефона (+ Vitest)
- [x] `src/server`: env (Zod), Prisma-клиент с pg-адаптером, конфиг Better Auth, route handler `/api/auth/[...all]`
- [x] CLAUDE.md, docs/PRD.md, docker-compose (Postgres), GitHub Actions CI, prettier
- [x] Миграция `20260919000000_init` применена на живой БД (нативный Postgres 16, роль `buscom`, БД `buscom_erp`; Docker на машине нет — см. DECISIONS). `prisma migrate status` — «up to date»
- [x] shadcn/ui (пресет `radix-nova`) + базовый layout: левое меню (Заказы, Клиенты, Товары, Администрирование — по правам), шапка с глобальным поиском и меню пользователя
- [x] Страница `/login` (email + пароль через Better Auth), `src/proxy.ts` редиректит без сессии на `/login?next=…` (кроме `/api/auth/*` и `/api/integrations/*`)
- [x] Хелпер `requireUser(roles?)` в `src/server/session.ts` — сессия + роль + `isActive`; `ForbiddenError` для 403
- [x] Сид `prisma/seed.ts` + скрипт `pnpm db:seed`: первый ADMIN из env (`SEED_ADMIN_EMAIL`, `SEED_ADMIN_NAME`, `SEED_ADMIN_PASSWORD`) и демо-каталог. Справочников в схеме пока нет — сеять нечего
- [x] Заглушки экранов `/orders`, `/customers`, `/products`, `/admin`, чтобы меню никуда не вело в 404; `/` редиректит на `/orders`
- [x] Проверено на живой БД: вход ADMIN'ом (cookie сессии ставится, `role` и `isActive` приходят), `/orders` без сессии → `/login?next=/orders`, `/login` с сессией → `/orders`, менеджер не видит пункт «Администрирование» и не открывает `/admin`, у деактивированного любой экран редиректит на `/login`
- [ ] Проверить UI глазами в браузере (проверка выше — curl'ом; вёрстку страниц никто не смотрел)
- [ ] Проверить, что CI на GitHub зелёный (локально `pnpm check` проходит; на GitHub не смотрели — `gh` не установлен)

## Этап 1 — Заказы (M1 + M8)

Критерии приёмки — PRD, «Экраны и критерии приёмки (P0)».

- [x] `src/domain/order/payment-status.ts` — вычисляемый статус оплаты, остаток к оплате, признак полной оплаты для автоперехода в PAID (+ 15 тестов)
- [x] `src/domain/order/discount.ts` — лимит скидки 10% от суммы до скидок, выше — только HEAD/ADMIN (+ 17 тестов)
- [x] `src/domain/sla.ts` — рабочие минуты пн–пт 09:00–18:00 МСК, нормативы по статусам, `slaState` и формат «2 раб. дн. 3 ч» (+ 28 тестов)
- [x] Сервис заказов в `src/server/orders/`: создание вручную, «взять себе», переназначение, смена статуса, правка позиций, оплаты, комментарии — каждое действие в транзакции с `OrderEvent`
- [x] Доменные правила к нему: `editing.ts` (кто и когда правит состав) и `reservation.ts` (когда заказ держит резерв) + 22 теста
- [x] Резерв остатка по правилам PRD: ставится при выходе из IN_PROGRESS вперёд, снимается при отмене и возврате в работу, при отгрузке списывается из `stock` и `reserved`. Товары «под заказ» и произвольные позиции не резервируются; `findStockShortages` считает нехватку для предупреждения в карточке
- [x] `/orders` — список: колонки по PRD, вкладки-виды со счётчиками, фильтры по статусу (множественный), менеджеру, источнику, периоду и оплате, поиск одним полем, пагинация по 50. Всё состояние в URL; склад открывает «К отгрузке», остальные — «Все»; просроченные подсвечены
- [x] Миграция `20260920000000_order_paid_and_sla_cache`: `Order.paidKopecks` и `Order.slaDueAt` — чтобы фильтры по оплате и просрочке были запросом, а не перебором (см. DECISIONS)
- [x] `/orders/[number]` — карточка: шапка со статусом, оплатой и кнопками ровно по `availableTransitions`, «Взять себе», переназначение руководителем, редактор позиций с подбором товара и предупреждением о лимите скидки, диалог отмены с причиной из справочника, блок оплат с подстановкой остатка, доставка, комментарий клиента и лента истории с полем внутреннего комментария
- [x] `/orders/new` — ручное создание: источник, поиск существующего клиента по телефону/имени/ИНН или новый, подбор позиций из каталога, доставка, скидка с предупреждением о лимите. Заказ сразу в IN_PROGRESS с автором в роли менеджера
- [x] `/admin/users` — список сотрудников, заведение с временным паролем, смена роли, отключение со сбросом сессий, выдача нового пароля. Себя отключить или разжаловать нельзя
- [x] `/admin/dictionaries` — причины отмены и транспортные компании (позиции гасятся, а не удаляются), лимит скидки, SLA по статусам, реквизиты продавца
- [x] `/admin/integration` — журнал входящих с фильтром по статусу, просмотром сырого JSON и кнопкой «Повторить» для упавших
- [x] Миграция `20260920010000_dictionaries_and_settings`: `DictionaryItem` и `Setting`. Настройки читаются через `src/server/settings/service.ts`, умолчания — в `src/domain/settings.ts`, поэтому пустая база работоспособна
- [x] `POST /api/integrations/site/orders` — контракт v1: HMAC-подпись сырого тела со сравнением в постоянном времени, сырой payload в `IntegrationInbox` до разбора, идемпотентность по `(source, externalId)`, поиск товара по `externalProductId` затем `sku`, предоплата с сайта, предупреждение о расхождении сумм. Все пять кодов ответа из PRD проверены живыми запросами (+ 23 теста)
- [x] PDF: счёт на оплату (с суммой прописью и реквизитами из настроек) и комплектовочный лист для склада. Генерирует pdfmake со шрифтом Roboto из самого пакета — бинарников в репозитории нет; кириллица в готовых файлах проверена
- [x] Сброс пароля по email: `/forgot-password` и `/reset-password`, письмо через nodemailer, SMTP из env. Без SMTP ссылка печатается в лог сервера, чтобы разработка не требовала почтового сервера. Проверено целиком: ссылка → смена → старый пароль не работает → токен одноразовый
- [x] Таблицы справочников и настроек заведены; лимит скидки и SLA из настроек реально применяются сервисом — проверено на живой БД (лимит 10% → 25% меняет приём скидки, SLA 5 минут меняет дедлайн)

## Сделано сверх этапа 1 (модули M2 и M3 из PRD)

- [x] `/products` — каталог: поиск, фильтры по категории и нехватке, заведение и правка товара, правка остатка складом прямо в таблице
- [x] `/customers` — база клиентов: поиск по имени, телефону, email и ИНН, фильтр по типу, сумма покупок по закрытым сделкам
- [x] `/customers/[id]` — карточка: данные с нормализацией телефона при сохранении, адреса доставки с одним адресом «по умолчанию», история заказов, слияние дублей вручную

## Что в этапе 1 осталось незакрытым

- [x] 29 интеграционных тестов прогнаны локально по базе `buscom_test` (2026-09-20): 207 тестов из 17 файлов зелёные, три прогона подряд. Миграции накатились на пустую базу с нуля — заодно проверены и они
- [ ] Посмотреть все экраны в браузере руками по критериям приёмки из PRD: разметка и поведение кнопок проверялись запросами, а не глазами
- [x] Экран 403 вместо стандартной ошибки Next: `requirePageUser` на страницах зовёт `forbidden()`, UI — `src/app/(app)/forbidden.tsx` внутри меню и шапки. Матрица прав прогнана на живой БД по всем четырём ролям (см. DECISIONS)
- [ ] Заполнить SMTP-переменные перед запуском, иначе письма со сбросом пароля только в логе
- [ ] Убрать из dev-БД 14 демо-заказов и 6 демо-клиентов перед боевым запуском

## Этап 2 и дальше

См. PRD, «Роадмап». Блокируется открытыми вопросами к заказчику (платформа сайта, способ приёма заказов).

## Известные проблемы и долги

- **Сессии деактивированного пользователя не отзываются.** `POST /api/auth/sign-in/email` напрямую выдаёт cookie и деактивированному (Better Auth про `isActive` не знает), но все экраны его разворачивают на `/login`, потому что `getSessionUser` такую сессию не признаёт. Когда появится экран пользователей — удалять сессии при деактивации (PRD: «деактивированный теряет все сессии сразу»).
- ~~**Экрана 403 нет.**~~ Сделан 2026-09-20: `requirePageUser` + `src/app/(app)/forbidden.tsx`, флаг `experimental.authInterrupts` включён. Правило: страница — `requirePageUser` (403-экран), действие или сервис — `requireUser` (`ForbiddenError` → тост). Route handlers по-прежнему отдают 403 сами.
- **Ограничение входа считает все попытки, а не только неудачные**, и живёт в памяти процесса (см. DECISIONS). PRD требует «10 неудачных за 15 минут» — при необходимости точного соблюдения нужен свой счётчик.
- **Разделы «Заказы», «Клиенты», «Товары» сделаны целиком** — заглушек в меню не осталось.
- **Поиск товара по совместимости требует названия модели целиком** («ГАЗель Next»): частичное совпадение внутри элемента массива Prisma не выражает. Если понадобится — отдельный запрос или вынести совместимость в таблицу.
- **Праздники РФ в SLA не учитываются** — считаются обычными рабочими днями (PRD: производственный календарь после MVP). Москва зашита как UTC+3 без перехода на летнее время; если его вернут, понадобится библиотека часовых поясов.
- **Вкладка «Просроченные» потребует расчёта SLA в SQL.** `slaState` считает в приложении, а список постраничный — на этапе 1 нужно либо хранить дедлайн в заказе, либо считать фильтр запросом.
- **Тесты серверного слоя стали возможны:** `vitest.config.mts` подменяет пакет `server-only` заглушкой (`src/test/server-only-stub.ts`), иначе он намеренно падает вне серверной среды Next.
- ~~**Интеграционные тесты локально не прогонялись.**~~ База `buscom_test` заведена 2026-09-20, роли `buscom` выдан `CREATEDB`. Прогон: `RUN_DB_TESTS=1 DATABASE_URL=postgresql://buscom:buscom@localhost:5432/buscom_test pnpm test`. Тесты в `*.db.test.ts` по-прежнему идут только при `RUN_DB_TESTS=1` и только по базе, в имени которой есть `test` — `resetDb` чистит все таблицы, и ошибка стоила бы данных.
- **Тесты по живой БД идут по очереди, а не параллельно.** На первом же прогоне два файла `*.db.test.ts` столкнулись на одной базе: `TRUNCATE` из одного вычищал данные другого — взаимоблокировка Postgres (40P01) и чужой `productId` в выборке. `vitest.config.mts` при `RUN_DB_TESTS=1` ставит `fileParallelism: false`. Цена — прогон 14–21 с вместо 1,3 с; обычный прогон без БД остался параллельным. Если файлов по БД станет заметно больше, дешевле дать каждому воркеру свою схему, чем держать всё последовательным.

- **В dev-БД израсходованы номера заказов 1 и 2** — проверочный прогон сервиса. В базе сейчас 12 демо-заказов (№3–14) для проверки списка; перед боевым запуском их надо убрать.
- **Карточку не кликали руками.** Разметка и права проверены запросами, но кнопки, диалоги и тосты в браузере никто не нажимал: расширение Claude для Chrome в сессии не подключено. Нужен проход человеком по критериям приёмки из PRD.
- **Причины отмены теперь в справочнике**; константа в `src/domain/order/cancel-reasons.ts` осталась как набор по умолчанию для пустой базы и как источник для сида.
- **Экраны администрирования не кликали руками** — как и карточку: проверены запросами и скриптом, но не в браузере.
- **Временный пароль показывается тостом** на экране пользователей, письмо не отправляется — сотрудник может сам запросить ссылку на `/forgot-password`.
- **SMTP не настроен.** Пока `SMTP_HOST` пуст, письма не уходят, а ссылка пишется в лог сервера с предупреждением. Перед запуском заполнить SMTP-переменные (см. `.env.example`).
- **Замер на 100 000 заказов сделан** (2026-09-20, production-сборка, локальный Postgres 16, данные потом удалены). p95 по HTTP: список 56 мс, вкладки 39–40 мс, фильтры 27 мс, страница 500 — 34 мс, клиенты 48 мс, товары 14 мс. Самое медленное — поиск одним полем: 210 мс по номеру, 284 мс по телефону. Требование ≤ 500 мс выполняется с запасом, но поиск — то место, которое первым упрётся в предел: он идёт `OR` по связанному клиенту, одним индексом это не закрыть. Если вырастет — вынести поисковую строку в отдельное поле заказа с GIN-индексом.
- **Поиск по email работает только при вводе адреса целиком** (с `@`); по части домена не ищет. По ИНН — точное совпадение. Если понадобится иначе, править `searchWhere` в `src/server/orders/list.ts`.
- ~~**`prisma migrate dev` на этой машине не работает**~~ — после `ALTER ROLE buscom CREATEDB` (2026-09-20) теневая БД создаётся, `prisma migrate dev` отвечает «Already in sync». Обходной путь через `migrate diff` + `migrate deploy` больше не нужен — новые миграции делать штатным `pnpm db:migrate`.
- ~~**Таблицы Better Auth написаны вручную**~~ — проверено на живой БД 2026-09-19: вход, сессия и дополнительные поля `role`/`isActive` работают. Одна ловушка уже стоила отладки: при входе Better Auth ищет аккаунт с `accountId === user.id`, поэтому в сиде туда пишется id пользователя, а не email.
- **`.claude/settings.json` запрещает `Read(.env.*)`** — это блокирует и `.env.example`: агент может дописывать в него, но не читает и не проверяет на дубли. Заменить на `Read(.env.local)` и `Read(.env.production)` (агенту правка собственных настроек запрещена — делает человек).
- **bus-com.ru не открывается из сессий с VPN** — данные о сайте брать у заказчика, а не скачивать.
- **Git-автор первых коммитов** — рабочий адрес из глобального git-конфига; для этого репозитория, возможно, стоит задать `git config user.email` локально.

## Открытые вопросы к заказчику

Список — в PRD, раздел «Открытые вопросы». Ответы записывать туда (и в живой документ), решения — в `docs/DECISIONS.md`.
